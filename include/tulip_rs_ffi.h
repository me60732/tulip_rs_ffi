#ifndef TULIP_RS_FFI_H
#define TULIP_RS_FFI_H

/* Warning, this file is autogenerated by cbindgen. Don't modify this manually. Run `cbindgen --config cbindgen.toml --output include/tulip_rs_ffi.h` to regenerate. */

#include <stdarg.h>
#include <stdbool.h>
#include <stdint.h>
#include <stdlib.h>
#include "tulip_rs_ffi_counts.h"
#include "tulip_rs_ffi_state_ids.h"

/**
 * C-ABI mirror of `tulip_rs::types::IndicatorType`.
 */
typedef enum CIndicatorType {
  C_INDICATOR_TYPE_TREND = 0,
  C_INDICATOR_TYPE_MOMENTUM = 1,
  C_INDICATOR_TYPE_VOLUME = 2,
  C_INDICATOR_TYPE_VOLATILITY = 3,
  C_INDICATOR_TYPE_PRICE = 4,
  C_INDICATOR_TYPE_CYCLE = 5,
  C_INDICATOR_TYPE_CANDLE_STICK = 6,
  C_INDICATOR_TYPE_MATH = 7,
  C_INDICATOR_TYPE_OTHER = 8,
} CIndicatorType;

/**
 * C-ABI mirror of `tulip_rs::types::DisplayType`.
 */
typedef enum CDisplayType {
  C_DISPLAY_TYPE_OVERLAY = 0,
  C_DISPLAY_TYPE_INDICATOR = 1,
  C_DISPLAY_TYPE_VOLUME = 2,
  C_DISPLAY_TYPE_PRICE = 3,
} CDisplayType;

/**
 * C-ABI mirror of `tulip_rs::types::IndicatorError`.
 */
typedef enum CIndicatorError {
  C_INDICATOR_ERROR_OK = 0,
  C_INDICATOR_ERROR_INVALID_INPUTS = 1,
  C_INDICATOR_ERROR_NOT_ENOUGH_DATA = 2,
  C_INDICATOR_ERROR_INVALID_OPTIONS = 3,
  C_INDICATOR_ERROR_INVALID_INDICATOR_STATE = 4,
} CIndicatorError;

/**
 * C-ABI mirror of `tulip_rs::candle_indicators::types::ForecastType`.
 */
enum CForecastType
#if defined(__cplusplus) || __STDC_VERSION__ >= 202311L
  : int32_t
#endif // defined(__cplusplus) || __STDC_VERSION__ >= 202311L
 {
  C_FORECAST_TYPE_BEARISH_REVERSAL = 0,
  C_FORECAST_TYPE_BULLISH_REVERSAL = 1,
  C_FORECAST_TYPE_BEARISH_CONTINUATION = 2,
  C_FORECAST_TYPE_BULLISH_CONTINUATION = 3,
  C_FORECAST_TYPE_BEARISH_REVERSAL_OR_CONTINUATION = 4,
  C_FORECAST_TYPE_BULLISH_REVERSAL_OR_CONTINUATION = 5,
};
#ifndef __cplusplus
#if __STDC_VERSION__ >= 202311L
typedef enum CForecastType CForecastType;
#else
typedef int32_t CForecastType;
#endif // __STDC_VERSION__ >= 202311L
#endif // __cplusplus

/**
 * Wire format selector for `tulip_state_serialize` (and the `format` byte
 * embedded in the blob header).
 */
enum CStateFormat
#if defined(__cplusplus) || __STDC_VERSION__ >= 202311L
  : uint32_t
#endif // defined(__cplusplus) || __STDC_VERSION__ >= 202311L
 {
  /**
   * Compact binary encoding; recommended for persistence. Handles all
   * f64 values including NaN/Inf.
   */
  C_STATE_FORMAT_BINCODE = 0,
  /**
   * Human-readable JSON via serde_json, for debugging/inspection only:
   * fails (null return) on non-finite f64s.
   */
  C_STATE_FORMAT_JSON = 1,
};
#ifndef __cplusplus
#if __STDC_VERSION__ >= 202311L
typedef enum CStateFormat CStateFormat;
#else
typedef uint32_t CStateFormat;
#endif // __STDC_VERSION__ >= 202311L
#endif // __cplusplus

/**
 * A C-ABI array of null-terminated strings: `len` pointers, each pointing to
 * a `\0`-terminated `c_char` buffer. Backing memory is intentionally leaked
 * (see `pack_info`) since it mirrors a `&'static` Rust string table that
 * lives for the process's lifetime -- callers must not free it.
 */
typedef struct CStringArray {
  const char *const *ptr;
  uintptr_t len;
} CStringArray;

/**
 * C-ABI mirror of `tulip_rs::types::DisplayGroup`. Backing memory
 * (including `outputs`) is intentionally leaked (mirrors `&'static` Rust
 * data) -- read it, do not free it. `offset` is null when the source
 * `Option<&'static str>` is `None`.
 */
typedef struct CDisplayGroup {
  const char *id;
  const char *label;
  enum CDisplayType display_type;
  const char *offset;
  struct CStringArray outputs;
} CDisplayGroup;

/**
 * A C-ABI array of `CDisplayGroup`s. Backing memory is intentionally
 * leaked (mirrors a `&'static` Rust slice) -- do not free it.
 */
typedef struct CDisplayGroupArray {
  const struct CDisplayGroup *ptr;
  uintptr_t len;
} CDisplayGroupArray;

/**
 * C-ABI mirror of `tulip_rs::types::Info`: static metadata describing an
 * indicator's name, type, inputs, options, (mandatory/optional) outputs,
 * and display groups. Returned by every `<name>_info()` function. Backing
 * memory is intentionally leaked (mirrors a `&'static` Rust string table)
 * -- do not free any field of this struct.
 */
typedef struct CIndicatorInfo {
  const char *name;
  const char *full_name;
  enum CIndicatorType indicator_type;
  struct CStringArray inputs;
  struct CStringArray options;
  struct CStringArray outputs;
  struct CStringArray optional_outputs;
  struct CDisplayGroupArray display_groups;
} CIndicatorInfo;

/**
 * Result of a fresh `*_indicator()` call: owns both the output buffers and
 * a freshly-boxed indicator state (for later `*_batch()` continuation).
 */
typedef struct CIndicatorResult {
  enum CIndicatorError error;
  double **outputs;
  uintptr_t *output_lens;
  uintptr_t num_outputs;
  /**
   * Opaque pointer to a boxed `<Name>::IndicatorState`. Null on error.
   */
  void *state;
} CIndicatorResult;

/**
 * Result of a `*_batch()` continuation call. The state pointer passed in
 * is mutated in place, so there's nothing to hand back here.
 */
typedef struct CBatchResult {
  enum CIndicatorError error;
  double **outputs;
  uintptr_t *output_lens;
  uintptr_t num_outputs;
} CBatchResult;

/**
 * Result of a SIMD `*_simd_by_assets`/`*_simd_by_options` call: N parallel
 * results, each with its own output rows and its own ordinary indicator
 * state (reusable with the existing `*_batch()`/`*_state_free()` functions).
 */
typedef struct CSimdResult {
  enum CIndicatorError error;
  /**
   * [num_results][num_outputs] -> pointer to an output row's f64 data.
   */
  double ***outputs;
  /**
   * [num_results][num_outputs] -> length of that output row.
   */
  uintptr_t **output_lens;
  /**
   * Number of output rows per result (mandatory + requested optional outputs).
   */
  uintptr_t num_outputs;
  /**
   * [num_results] -> opaque boxed state pointer, each usable with the
   * matching indicator's existing `*_batch()`/`*_state_free()` functions.
   */
  void **states;
  /**
   * N: number of parallel results (assets or option sets).
   */
  uintptr_t num_results;
} CSimdResult;

/**
 * C-ABI mirror of `tulip_rs::candle_indicators::types::CandleInfo`, with
 * the pattern's table `id` attached. All string fields point to leaked,
 * process-lifetime buffers -- read them, don't free them.
 *
 * A `u32::MAX` `id` (with null strings) is returned for out-of-range lookups.
 */
typedef struct CCandlePatternInfo {
  uint32_t id;
  const char *name;
  const char *full_name;
  const char *japanese_name;
  CForecastType forecast;
  uint32_t bars;
} CCandlePatternInfo;

/**
 * Result of a fresh `candlestick_indicator()` call: CSR-packed pattern
 * output plus a freshly-boxed continuation state.
 */
typedef struct CCandleStickResult {
  enum CIndicatorError error;
  uintptr_t num_bars;
  uintptr_t total_patterns;
  /**
   * `num_bars + 1` entries; row offsets into `pattern_ids`.
   */
  uint32_t *bar_offsets;
  /**
   * `total_patterns` entries; pattern table ids.
   */
  uint32_t *pattern_ids;
  /**
   * Opaque pointer to a boxed `CandleStick::IndicatorState`. Null on error.
   */
  void *state;
} CCandleStickResult;

/**
 * Result of a `candlestick_batch()` continuation call (same CSR layout as
 * `CCandleStickResult`, minus the state -- the state passed in is mutated
 * in place).
 */
typedef struct CCandleStickBatchResult {
  enum CIndicatorError error;
  uintptr_t num_bars;
  uintptr_t total_patterns;
  uint32_t *bar_offsets;
  uint32_t *pattern_ids;
} CCandleStickBatchResult;

/**
 * A C-ABI byte buffer owned by the caller after being returned from Rust.
 * `ptr` is null on error (or when `len == 0` is returned — no: on error only).
 * Must be released exactly once with `tulip_ffi_bytes_free`.
 */
typedef struct CBytes {
  uint8_t *ptr;
  uintptr_t len;
} CBytes;

#ifdef __cplusplus
extern "C" {
#endif // __cplusplus

/**
 * Returns static metadata about the `ad` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Ad::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo ad_info(void);

/**
 * Returns the minimum number of bars `ad` needs to produce any output at
 * all, given `options`.
 */
uintptr_t ad_min_data(const double *_options);

/**
 * Runs `ad` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (4) pointers, in order:
 * `high, low, close, volume`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (0) values (no options).
 *
 * Returns the mandatory `ad` output plus any requested optional outputs
 * (none for this indicator) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CIndicatorResult ad_indicator(const double *const *inputs,
                                     uintptr_t data_len,
                                     const double *options,
                                     const bool *optional_outputs,
                                     uintptr_t numoptional);

/**
 * Continues an `ad` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `ad_indicator()` and not yet have been passed to `ad_state_free()`.
 *
 * `inputs` must point to `INPUTS` (4) pointers (`high, low, close,
 * volume`), each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `ad_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CBatchResult ad_batch(void *state,
                             const double *const *inputs,
                             uintptr_t data_len,
                             const bool *optional_outputs,
                             uintptr_t numoptional);

/**
 * Frees a state handle returned by `ad_indicator()` (or one of the
 * `states[i]` entries from `ad_simd_by_assets()`).
 * Call this once you're done streaming (after your last `ad_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `ad_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void ad_state_free(void *state);

/**
 * Computes AD for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (4) pointers (`high, low, close, volume`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (0) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `ad` continuation state (reusable
 * with `ad_batch()`/`ad_state_free()`). Free each state via
 * `ad_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 */
struct CSimdResult ad_simd_by_assets(const double *const *const *inputs,
                                     uintptr_t num_assets,
                                     uintptr_t data_len,
                                     const double *options,
                                     const bool *optional_outputs,
                                     uintptr_t numoptional);

/**
 * Returns static metadata about the `adaptivemsw` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `AdaptiveMSW::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo adaptivemsw_info(void);

/**
 * Returns the minimum number of bars `adaptivemsw` needs to produce any output at
 * all, given `options`.
 */
uintptr_t adaptivemsw_min_data(const double *_options);

/**
 * Runs `adaptivemsw` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (0) values (no options).
 *
 * Returns the mandatory `sine`/`lead_sine` outputs plus any requested optional outputs
 * (`dc_period`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s (no options, so this can be null or any pointer).
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult adaptivemsw_indicator(const double *const *inputs,
                                              uintptr_t data_len,
                                              const double *options,
                                              const bool *optional_outputs,
                                              uintptr_t numoptional);

/**
 * Continues an `adaptivemsw` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `adaptivemsw_indicator()` and not yet have been passed to `adaptivemsw_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `adaptivemsw_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult adaptivemsw_batch(void *state,
                                      const double *const *inputs,
                                      uintptr_t data_len,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Frees a state handle returned by `adaptivemsw_indicator()` (or one of the
 * `states[i]` entries from `adaptivemsw_simd_by_assets()`).
 * Call this once you're done streaming (after your last `adaptivemsw_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `adaptivemsw_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void adaptivemsw_state_free(void *state);

/**
 * Computes AdaptiveMSW for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (0) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `adaptivemsw` continuation state (reusable
 * with `adaptivemsw_batch()`/`adaptivemsw_state_free()`). Free each state via
 * `adaptivemsw_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 */
struct CSimdResult adaptivemsw_simd_by_assets(const double *const *const *inputs,
                                              uintptr_t num_assets,
                                              uintptr_t data_len,
                                              const double *options,
                                              const bool *optional_outputs,
                                              uintptr_t numoptional);

/**
 * Returns static metadata about the `adosc` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Adosc::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo adosc_info(void);

/**
 * Returns the minimum number of bars `adosc` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (2) valid `f64`s.
 */
uintptr_t adosc_min_data(const double *options);

/**
 * Runs `adosc` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (4) pointers, in order:
 * `high, low, close, volume`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (2) values: `short_period, long_period`.
 *
 * Returns the mandatory `adosc` output plus any requested optional outputs
 * (`short_ema`, `long_ema`, `ad`, in that fixed order) and a fresh
 * continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult adosc_indicator(const double *const *inputs,
                                        uintptr_t data_len,
                                        const double *options,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Continues an `adosc` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `adosc_indicator()` and not yet have been passed to `adosc_state_free()`.
 *
 * `inputs` must point to `INPUTS` (4) pointers (`high, low, close,
 * volume`), each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `adosc_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult adosc_batch(void *state,
                                const double *const *inputs,
                                uintptr_t data_len,
                                const bool *optional_outputs,
                                uintptr_t numoptional);

/**
 * Frees a state handle returned by `adosc_indicator()` (or one of the
 * `states[i]` entries from `adosc_simd_by_assets()`/`adosc_simd_by_options()`).
 * Call this once you're done streaming (after your last `adosc_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `adosc_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void adosc_state_free(void *state);

/**
 * Computes ADOSC for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (4) pointers (`high, low, close, volume`), each `data_len` `f64`s
 * long. `options` must point to `OPTIONS` (2) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `adosc` continuation state (reusable
 * with `adosc_batch()`/`adosc_state_free()`). Free each state via
 * `adosc_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult adosc_simd_by_assets(const double *const *const *inputs,
                                        uintptr_t num_assets,
                                        uintptr_t data_len,
                                        const double *options,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Computes ADOSC for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (4) pointers (`high, low, close,
 * volume`), each `data_len` `f64`s long, shared across all option sets.
 * `options` must point to `num_option_sets` pointers, each itself
 * pointing to `OPTIONS` (2) values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `adosc` continuation state
 * (reusable with `adosc_batch()`/`adosc_state_free()`). Free each state
 * via `adosc_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult adosc_simd_by_options(const double *const *inputs,
                                         uintptr_t data_len,
                                         const double *const *options,
                                         uintptr_t num_option_sets,
                                         const bool *optional_outputs,
                                         uintptr_t numoptional);

/**
 * Returns static metadata about the `adx` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Adx::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo adx_info(void);

/**
 * Returns the minimum number of bars `adx` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t adx_min_data(const double *options);

/**
 * Runs `adx` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers, in order:
 * `high, low, close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) values: `period`.
 *
 * Returns the mandatory `adx` output plus any requested optional outputs
 * (`dx`, `atr`, `tr`, in that fixed order) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult adx_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues an `adx` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `adx_indicator()` and not yet have been passed to `adx_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `adx_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult adx_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `adx_indicator()` (or one of the
 * `states[i]` entries from `adx_simd_by_assets()`/`adx_simd_by_options()`).
 * Call this once you're done streaming (after your last `adx_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `adx_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void adx_state_free(void *state);

/**
 * Computes ADX for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s
 * long. `options` must point to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `adx` continuation state (reusable
 * with `adx_batch()`/`adx_state_free()`). Free each state via
 * `adx_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult adx_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Computes ADX for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`),
 * each `data_len` `f64`s long, shared across all option sets.
 * `options` must point to `num_option_sets` pointers, each itself
 * pointing to `OPTIONS` (1) values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `adx` continuation state
 * (reusable with `adx_batch()`/`adx_state_free()`). Free each state
 * via `adx_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult adx_simd_by_options(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *const *options,
                                       uintptr_t num_option_sets,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Returns static metadata about the `adxr` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Adxr::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo adxr_info(void);

/**
 * Returns the minimum number of bars `adxr` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t adxr_min_data(const double *options);

/**
 * Runs `adxr` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers, in order:
 * `high, low, close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) values: `period`.
 *
 * Returns the mandatory `adxr` output plus any requested optional outputs
 * (`adx`, `dx`, `atr`, `tr`, in that fixed order) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult adxr_indicator(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Continues an `adxr` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `adxr_indicator()` and not yet have been passed to `adxr_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `adxr_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult adxr_batch(void *state,
                               const double *const *inputs,
                               uintptr_t data_len,
                               const bool *optional_outputs,
                               uintptr_t numoptional);

/**
 * Frees a state handle returned by `adxr_indicator()` (or one of the
 * `states[i]` entries from `adxr_simd_by_assets()`/`adxr_simd_by_options()`).
 * Call this once you're done streaming (after your last `adxr_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `adxr_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void adxr_state_free(void *state);

/**
 * Computes ADXR for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s
 * long. `options` must point to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `adxr` continuation state (reusable
 * with `adxr_batch()`/`adxr_state_free()`). Free each state via
 * `adxr_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult adxr_simd_by_assets(const double *const *const *inputs,
                                       uintptr_t num_assets,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Computes ADXR for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`),
 * each `data_len` `f64`s long, shared across all option sets.
 * `options` must point to `num_option_sets` pointers, each itself
 * pointing to `OPTIONS` (1) values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `adxr` continuation state
 * (reusable with `adxr_batch()`/`adxr_state_free()`). Free each state
 * via `adxr_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult adxr_simd_by_options(const double *const *inputs,
                                        uintptr_t data_len,
                                        const double *const *options,
                                        uintptr_t num_option_sets,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Returns static metadata about the `ao` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Ao::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo ao_info(void);

/**
 * Returns the minimum number of bars `ao` needs to produce any output at
 * all, given `options`.
 */
uintptr_t ao_min_data(const double *_options);

/**
 * Runs `ao` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (2) pointers, in order:
 * `high, low`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (0) values (no options).
 *
 * Returns the mandatory `ao` output plus any requested optional outputs
 * (`short_sma`, `long_sma`, `medprice`, in that fixed order) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CIndicatorResult ao_indicator(const double *const *inputs,
                                     uintptr_t data_len,
                                     const double *options,
                                     const bool *optional_outputs,
                                     uintptr_t numoptional);

/**
 * Continues an `ao` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `ao_indicator()` and not yet have been passed to `ao_state_free()`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`high, low`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `ao_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CBatchResult ao_batch(void *state,
                             const double *const *inputs,
                             uintptr_t data_len,
                             const bool *optional_outputs,
                             uintptr_t numoptional);

/**
 * Frees a state handle returned by `ao_indicator()` (or one of the
 * `states[i]` entries from `ao_simd_by_assets()`).
 * Call this once you're done streaming (after your last `ao_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `ao_indicator()`
 * or found in a `CSimdResult::states` array (from `ao_simd_by_assets()`),
 * and must not be used again after this call.
 */
void ao_state_free(void *state);

/**
 * Computes AO for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (2) pointers (`high, low`), each `data_len` `f64`s
 * long. `options` must point to `OPTIONS` (0) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `ao` continuation state (reusable
 * with `ao_batch()`/`ao_state_free()`). Free each state via
 * `ao_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 */
struct CSimdResult ao_simd_by_assets(const double *const *const *inputs,
                                     uintptr_t num_assets,
                                     uintptr_t data_len,
                                     const double *options,
                                     const bool *optional_outputs,
                                     uintptr_t numoptional);

/**
 * Returns static metadata about the `apo` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Apo::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo apo_info(void);

/**
 * Returns the minimum number of bars `apo` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (2) valid `f64`s.
 */
uintptr_t apo_min_data(const double *options);

/**
 * Runs `apo` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `close`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (2) values: `short_period, long_period`.
 *
 * Returns the mandatory `apo` output plus any requested optional outputs
 * (`short_ema`, `long_ema`, in that fixed order) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult apo_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues an `apo` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `apo_indicator()` and not yet have been passed to `apo_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `close`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `apo_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult apo_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `apo_indicator()` (or one of the
 * `states[i]` entries from `apo_simd_by_assets()`/`apo_simd_by_options()`).
 * Call this once you're done streaming (after your last `apo_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `apo_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void apo_state_free(void *state);

/**
 * Computes APO for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`close`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (2) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `apo` continuation state (reusable
 * with `apo_batch()`/`apo_state_free()`). Free each state via
 * `apo_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult apo_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Computes APO for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`close`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (2)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `apo` continuation state
 * (reusable with `apo_batch()`/`apo_state_free()`). Free each state via
 * `apo_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult apo_simd_by_options(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *const *options,
                                       uintptr_t num_option_sets,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Returns static metadata about the `aroon` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Aroon::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo aroon_info(void);

/**
 * Returns the minimum number of bars `aroon` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t aroon_min_data(const double *options);

/**
 * Runs `aroon` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (2) pointers, in order:
 * `high, low`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `aroon_down`/`aroon_up` outputs plus any requested
 * optional outputs (none for Aroon).
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult aroon_indicator(const double *const *inputs,
                                        uintptr_t data_len,
                                        const double *options,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Continues an `aroon` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `aroon_indicator()` and not yet have been passed to `aroon_state_free()`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`high, low`), each `data_len`
 * `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `aroon_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult aroon_batch(void *state,
                                const double *const *inputs,
                                uintptr_t data_len,
                                const bool *optional_outputs,
                                uintptr_t numoptional);

/**
 * Frees a state handle returned by `aroon_indicator()` (or one of the
 * `states[i]` entries from `aroon_simd_by_assets()`/`aroon_simd_by_options()`).
 * Call this once you're done streaming (after your last `aroon_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `aroon_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void aroon_state_free(void *state);

/**
 * Computes AROON for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (2) pointers (`high, low`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) shared value.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `aroon` continuation state (reusable
 * with `aroon_batch()`/`aroon_state_free()`). Free each state via
 * `aroon_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult aroon_simd_by_assets(const double *const *const *inputs,
                                        uintptr_t num_assets,
                                        uintptr_t data_len,
                                        const double *options,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Computes AROON for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`high, low`), each `data_len`
 * `f64`s long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * value.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `aroon` continuation state
 * (reusable with `aroon_batch()`/`aroon_state_free()`). Free each state via
 * `aroon_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult aroon_simd_by_options(const double *const *inputs,
                                         uintptr_t data_len,
                                         const double *const *options,
                                         uintptr_t num_option_sets,
                                         const bool *optional_outputs,
                                         uintptr_t numoptional);

/**
 * Returns static metadata about the `aroonosc` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `AroonOsc::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo aroonosc_info(void);

/**
 * Returns the minimum number of bars `aroonosc` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t aroonosc_min_data(const double *options);

/**
 * Runs `aroonosc` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (2) pointers, in order:
 * `high, low`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `aroonosc` output plus any requested optional outputs
 * (`aroon_down`, `aroon_up`, in that fixed order).
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult aroonosc_indicator(const double *const *inputs,
                                           uintptr_t data_len,
                                           const double *options,
                                           const bool *optional_outputs,
                                           uintptr_t numoptional);

/**
 * Continues an `aroonosc` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `aroonosc_indicator()` and not yet have been passed to `aroonosc_state_free()`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`high, low`), each `data_len`
 * `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `aroonosc_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult aroonosc_batch(void *state,
                                   const double *const *inputs,
                                   uintptr_t data_len,
                                   const bool *optional_outputs,
                                   uintptr_t numoptional);

/**
 * Frees a state handle returned by `aroonosc_indicator()` (or one of the
 * `states[i]` entries from `aroonosc_simd_by_assets()`/`aroonosc_simd_by_options()`).
 * Call this once you're done streaming (after your last `aroonosc_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `aroonosc_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void aroonosc_state_free(void *state);

/**
 * Computes AROONOSC for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (2) pointers (`high, low`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) shared value.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `aroonosc` continuation state (reusable
 * with `aroonosc_batch()`/`aroonosc_state_free()`). Free each state via
 * `aroonosc_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult aroonosc_simd_by_assets(const double *const *const *inputs,
                                           uintptr_t num_assets,
                                           uintptr_t data_len,
                                           const double *options,
                                           const bool *optional_outputs,
                                           uintptr_t numoptional);

/**
 * Computes AROONOSC for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`high, low`), each `data_len`
 * `f64`s long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * value.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `aroonosc` continuation state
 * (reusable with `aroonosc_batch()`/`aroonosc_state_free()`). Free each state via
 * `aroonosc_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult aroonosc_simd_by_options(const double *const *inputs,
                                            uintptr_t data_len,
                                            const double *const *options,
                                            uintptr_t num_option_sets,
                                            const bool *optional_outputs,
                                            uintptr_t numoptional);

/**
 * Returns static metadata about the `atr` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Atr::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo atr_info(void);

/**
 * Returns the minimum number of bars `atr` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t atr_min_data(const double *options);

/**
 * Runs `atr` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers, in order:
 * `high, low, close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `atr` output plus any requested optional outputs
 * (`tr`, in that fixed order).
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult atr_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues an `atr` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `atr_indicator()` and not yet have been passed to `atr_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`), each
 * `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `atr_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult atr_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `atr_indicator()` (or one of the
 * `states[i]` entries from `atr_simd_by_assets()`/`atr_simd_by_options()`).
 * Call this once you're done streaming (after your last `atr_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `atr_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void atr_state_free(void *state);

/**
 * Computes ATR for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) shared value.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `atr` continuation state (reusable
 * with `atr_batch()`/`atr_state_free()`). Free each state via
 * `atr_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult atr_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Computes ATR for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`), each
 * `data_len` `f64`s long, shared across all option sets. `options` must point
 * to `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * value.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `atr` continuation state
 * (reusable with `atr_batch()`/`atr_state_free()`). Free each state via
 * `atr_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult atr_simd_by_options(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *const *options,
                                       uintptr_t num_option_sets,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Returns static metadata about the `avgprice` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `AvgPrice::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo avgprice_info(void);

/**
 * Returns the minimum number of bars `avgprice` needs to produce any output at
 * all, given `options`.
 */
uintptr_t avgprice_min_data(const double *_options);

/**
 * Runs `avgprice` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (4) pointers, in order:
 * `open, high, low, close`, each `data_len` `f64`s long.
 * `options` is empty for this indicator (OPTIONS = 0).
 *
 * Returns the mandatory `avgprice` output plus any requested optional outputs
 * (none for AvgPrice).
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult avgprice_indicator(const double *const *inputs,
                                           uintptr_t data_len,
                                           const double *_options,
                                           const bool *optional_outputs,
                                           uintptr_t numoptional);

/**
 * Continues an `avgprice` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `avgprice_indicator()` and not yet have been passed to `avgprice_state_free()`.
 *
 * `inputs` must point to `INPUTS` (4) pointers (`open, high, low, close`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `avgprice_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult avgprice_batch(void *state,
                                   const double *const *inputs,
                                   uintptr_t data_len,
                                   const bool *optional_outputs,
                                   uintptr_t numoptional);

/**
 * Frees a state handle returned by `avgprice_indicator()` (or one of the
 * `states[i]` entries from `avgprice_simd_by_assets()`).
 * Call this once you're done streaming (after your last `avgprice_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `avgprice_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void avgprice_state_free(void *state);

/**
 * Computes AVGPRICE for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (4) pointers (`open, high, low, close`), each `data_len` `f64`s
 * long. `options` is empty for this indicator.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `avgprice` continuation state (reusable
 * with `avgprice_batch()`/`avgprice_state_free()`). Free each state via
 * `avgprice_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult avgprice_simd_by_assets(const double *const *const *inputs,
                                           uintptr_t num_assets,
                                           uintptr_t data_len,
                                           const double *_options,
                                           const bool *optional_outputs,
                                           uintptr_t numoptional);

/**
 * Returns static metadata about the `bbands` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `BBands::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo bbands_info(void);

/**
 * Returns the minimum number of bars `bbands` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (2) valid `f64`s.
 */
uintptr_t bbands_min_data(const double *options);

/**
 * Runs `bbands` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (2) values: `period, std_dev`.
 *
 * Returns the mandatory `bbands_lower`/`bbands_middle`/`bbands_upper` outputs
 * plus any requested optional outputs and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult bbands_indicator(const double *const *inputs,
                                         uintptr_t data_len,
                                         const double *options,
                                         const bool *optional_outputs,
                                         uintptr_t numoptional);

/**
 * Continues a `bbands` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `bbands_indicator()` and not yet have been passed to `bbands_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `bbands_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult bbands_batch(void *state,
                                 const double *const *inputs,
                                 uintptr_t data_len,
                                 const bool *optional_outputs,
                                 uintptr_t numoptional);

/**
 * Frees a state handle returned by `bbands_indicator()` (or one of the
 * `states[i]` entries from `bbands_simd_by_assets()`/`bbands_simd_by_options()`).
 * Call this once you're done streaming (after your last `bbands_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `bbands_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void bbands_state_free(void *state);

/**
 * Computes BBands for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (2) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `bbands` continuation state (reusable
 * with `bbands_batch()`/`bbands_state_free()`). Free each state via
 * `bbands_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult bbands_simd_by_assets(const double *const *const *inputs,
                                         uintptr_t num_assets,
                                         uintptr_t data_len,
                                         const double *options,
                                         const bool *optional_outputs,
                                         uintptr_t numoptional);

/**
 * Computes BBands for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (2)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `bbands` continuation state
 * (reusable with `bbands_batch()`/`bbands_state_free()`). Free each state via
 * `bbands_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult bbands_simd_by_options(const double *const *inputs,
                                          uintptr_t data_len,
                                          const double *const *options,
                                          uintptr_t num_option_sets,
                                          const bool *optional_outputs,
                                          uintptr_t numoptional);

/**
 * Returns static metadata about the `bop` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Bop::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo bop_info(void);

/**
 * Returns the minimum number of bars `bop` needs to produce any output at
 * all, given `options`.
 */
uintptr_t bop_min_data(const double *_options);

/**
 * Runs `bop` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (4) pointers, in order:
 * `open, high, low, close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (0) values (no options).
 *
 * Returns the mandatory `bop` output plus any requested optional outputs
 * and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult bop_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues a `bop` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `bop_indicator()` and not yet have been passed to `bop_state_free()`.
 *
 * `inputs` must point to `INPUTS` (4) pointers (`open, high, low, close`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `bop_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult bop_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `bop_indicator()` (or one of the
 * `states[i]` entries from `bop_simd_by_assets()`).
 * Call this once you're done streaming (after your last `bop_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `bop_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void bop_state_free(void *state);

/**
 * Computes BOP for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (4) pointers (`open, high, low, close`), each `data_len` `f64`s
 * long. `options` must point to `OPTIONS` (0) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `bop` continuation state (reusable
 * with `bop_batch()`/`bop_state_free()`). Free each state via
 * `bop_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 */
struct CSimdResult bop_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Returns static metadata about the `candlestick` indicator: its name,
 * input names, option names, and output names, mirroring
 * `CandleStick::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo candlestick_info(void);

/**
 * Returns the minimum number of bars `candlestick` needs to produce any
 * output at all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (3) valid `f64`s.
 */
uintptr_t candlestick_min_data(const double *options);

/**
 * Number of detectable candlestick patterns (ids `0..len`, see
 * `candlestick_pattern_info()`).
 */
uintptr_t candlestick_num_patterns(void);

/**
 * Metadata for the pattern with table id `id`. Returns a struct with
 * `id == u32::MAX` and null strings if `id` is out of range.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CCandlePatternInfo candlestick_pattern_info(uint32_t id);

/**
 * All pattern short names (by table id) as a C array of null-terminated
 * strings, for the common "I just want the name" case. Backing memory is
 * leaked -- read it, don't free it.
 */
struct CStringArray candlestick_pattern_names(void);

/**
 * Runs `candlestick` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (4) pointers: `open`, `high`, `low`,
 * `close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (3) values: `candle_period`,
 * `trend_period`, `trend_signal_period`.
 * `forecast` filters the detected patterns by forecast direction
 * (`0..=5` = `CForecastType` variant; `-1` or anything else = no filter).
 *
 * Returns the CSR-packed pattern output (see the module docs) plus a fresh
 * continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 */
struct CCandleStickResult candlestick_indicator(const double *const *inputs,
                                                uintptr_t data_len,
                                                const double *options,
                                                int32_t forecast);

/**
 * Continues a `candlestick` calculation from `state`, mutating it in place
 * so it's ready for the next call. `state` must have come from
 * `candlestick_indicator()` and not yet have been passed to
 * `candlestick_state_free()`.
 *
 * `inputs` must point to `INPUTS` (4) pointers: `open`, `high`, `low`,
 * `close`, each `data_len` `f64`s long (just the new bars).
 * `forecast` filters the detected patterns (see `candlestick_indicator()`).
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `candlestick_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CCandleStickBatchResult candlestick_batch(void *state,
                                                 const double *const *inputs,
                                                 uintptr_t data_len,
                                                 int32_t forecast);

/**
 * Frees the CSR buffers owned by a `CCandleStickResult`. Does **not** touch
 * `state` -- call `candlestick_state_free()` for that.
 *
 * # Safety
 * Must be called at most once per `CCandleStickResult`, and only on a value
 * actually returned by `candlestick_indicator()`.
 */
void candlestick_result_free(struct CCandleStickResult result);

/**
 * Frees the CSR buffers owned by a `CCandleStickBatchResult`.
 *
 * # Safety
 * Must be called at most once per `CCandleStickBatchResult`, and only on a
 * value actually returned by `candlestick_batch()`.
 */
void candlestick_batch_result_free(struct CCandleStickBatchResult result);

/**
 * Frees a state handle returned by `candlestick_indicator()`. Call this
 * once you're done streaming (after your last `candlestick_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by
 * `candlestick_indicator()`, and must not be used again after this call.
 */
void candlestick_state_free(void *state);

/**
 * Returns static metadata about the `ccfisher` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `CcFisher::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo ccfisher_info(void);

/**
 * Returns the minimum number of bars `ccfisher` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t ccfisher_min_data(const double *options);

/**
 * Runs `ccfisher` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `alpha`.
 *
 * Returns the mandatory `fisher`/`signal` outputs plus any requested optional
 * outputs (`trendmode`, `cycle`, `peak`, in that fixed order) and a fresh
 * continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult ccfisher_indicator(const double *const *inputs,
                                           uintptr_t data_len,
                                           const double *options,
                                           const bool *optional_outputs,
                                           uintptr_t numoptional);

/**
 * Continues a `ccfisher` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `ccfisher_indicator()` and not yet have been passed to `ccfisher_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `ccfisher_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult ccfisher_batch(void *state,
                                   const double *const *inputs,
                                   uintptr_t data_len,
                                   const bool *optional_outputs,
                                   uintptr_t numoptional);

/**
 * Frees a state handle returned by `ccfisher_indicator()` (or one of the
 * `states[i]` entries from `ccfisher_simd_by_assets()`/`ccfisher_simd_by_options()`).
 * Call this once you're done streaming (after your last `ccfisher_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `ccfisher_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void ccfisher_state_free(void *state);

/**
 * Computes CCFisher for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `ccfisher` continuation state (reusable
 * with `ccfisher_batch()`/`ccfisher_state_free()`). Free each state via
 * `ccfisher_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult ccfisher_simd_by_assets(const double *const *const *inputs,
                                           uintptr_t num_assets,
                                           uintptr_t data_len,
                                           const double *options,
                                           const bool *optional_outputs,
                                           uintptr_t numoptional);

/**
 * Computes CCFisher for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `ccfisher` continuation state
 * (reusable with `ccfisher_batch()`/`ccfisher_state_free()`). Free each state via
 * `ccfisher_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult ccfisher_simd_by_options(const double *const *inputs,
                                            uintptr_t data_len,
                                            const double *const *options,
                                            uintptr_t num_option_sets,
                                            const bool *optional_outputs,
                                            uintptr_t numoptional);

/**
 * Returns static metadata about the `cci` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Cci::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo cci_info(void);

/**
 * Returns the minimum number of bars `cci` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t cci_min_data(const double *options);

/**
 * Runs `cci` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers, in order:
 * `high, low, close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `cci` output plus any requested optional outputs
 * (`sma`, `md`, `typprice`, in that fixed order) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult cci_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues a `cci` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `cci_indicator()` and not yet have been passed to `cci_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `cci_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult cci_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `cci_indicator()` (or one of the
 * `states[i]` entries from `cci_simd_by_assets()`/`cci_simd_by_options()`).
 * Call this once you're done streaming (after your last `cci_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `cci_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void cci_state_free(void *state);

/**
 * Computes CCI for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `cci` continuation state (reusable
 * with `cci_batch()`/`cci_state_free()`). Free each state via
 * `cci_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult cci_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Computes CCI for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`),
 * each `data_len` `f64`s long, shared across all option sets. `options`
 * must point to `num_option_sets` pointers, each itself pointing to
 * `OPTIONS` (1) values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `cci` continuation state
 * (reusable with `cci_batch()`/`cci_state_free()`). Free each state via
 * `cci_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult cci_simd_by_options(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *const *options,
                                       uintptr_t num_option_sets,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Returns static metadata about the `chaikinmf` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `ChaikinMf::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo chaikinmf_info(void);

/**
 * Returns the minimum number of bars `chaikinmf` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t chaikinmf_min_data(const double *options);

/**
 * Runs `chaikinmf` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (4) pointers, in order:
 * `high, low, close, volume`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `cmf` output plus any requested optional outputs
 * and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult chaikinmf_indicator(const double *const *inputs,
                                            uintptr_t data_len,
                                            const double *options,
                                            const bool *optional_outputs,
                                            uintptr_t numoptional);

/**
 * Continues a `chaikinmf` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `chaikinmf_indicator()` and not yet have been passed to `chaikinmf_state_free()`.
 *
 * `inputs` must point to `INPUTS` (4) pointers (`high, low, close, volume`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `chaikinmf_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult chaikinmf_batch(void *state,
                                    const double *const *inputs,
                                    uintptr_t data_len,
                                    const bool *optional_outputs,
                                    uintptr_t numoptional);

/**
 * Frees a state handle returned by `chaikinmf_indicator()` (or one of the
 * `states[i]` entries from `chaikinmf_simd_by_assets()`/`chaikinmf_simd_by_options()`).
 * Call this once you're done streaming (after your last `chaikinmf_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `chaikinmf_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void chaikinmf_state_free(void *state);

/**
 * Computes Chaikin Money Flow for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (4) pointers (`high, low, close, volume`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `chaikinmf` continuation state (reusable
 * with `chaikinmf_batch()`/`chaikinmf_state_free()`). Free each state via
 * `chaikinmf_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult chaikinmf_simd_by_assets(const double *const *const *inputs,
                                            uintptr_t num_assets,
                                            uintptr_t data_len,
                                            const double *options,
                                            const bool *optional_outputs,
                                            uintptr_t numoptional);

/**
 * Computes Chaikin Money Flow for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (4) pointers (`high, low, close, volume`),
 * each `data_len` `f64`s long, shared across all option sets. `options`
 * must point to `num_option_sets` pointers, each itself pointing to
 * `OPTIONS` (1) values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `chaikinmf` continuation state
 * (reusable with `chaikinmf_batch()`/`chaikinmf_state_free()`). Free each state via
 * `chaikinmf_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult chaikinmf_simd_by_options(const double *const *inputs,
                                             uintptr_t data_len,
                                             const double *const *options,
                                             uintptr_t num_option_sets,
                                             const bool *optional_outputs,
                                             uintptr_t numoptional);

/**
 * Runs `chandelierexit` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers, in order:
 * `high, low, close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (2) values: `period, step`.
 *
 * Returns the mandatory `long`/`short` outputs plus any requested optional
 * outputs (`atr`, `tr`, `min`, `max`, in that fixed order) and a fresh
 * continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `num_optional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult chandelierexit_indicator(const double *const *inputs,
                                                 uintptr_t data_len,
                                                 const double *options,
                                                 const bool *optional_outputs,
                                                 uintptr_t num_optional);

/**
 * Continues a `chandelierexit` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `chandelierexit_indicator()` and not yet have been passed to
 * `chandelierexit_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `chandelierexit_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `num_optional` valid
 *   `bool`s.
 */
struct CBatchResult chandelierexit_batch(void *state,
                                         const double *const *inputs,
                                         uintptr_t data_len,
                                         const bool *optional_outputs,
                                         uintptr_t num_optional);

/**
 * Frees a state handle returned by `chandelierexit_indicator()` (or one of the
 * `states[i]` entries from `chandelierexit_simd_by_assets()`/`chandelierexit_simd_by_options()`).
 * Call this once you're done streaming (after your last `chandelierexit_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `chandelierexit_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void chandelierexit_state_free(void *state);

/**
 * Computes Chandelier Exit for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (2) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `chandelierexit` continuation state (reusable
 * with `chandelierexit_batch()`/`chandelierexit_state_free()`). Free each state via
 * `chandelierexit_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `num_optional` valid
 *   `bool`s.
 */
struct CSimdResult chandelierexit_simd_by_assets(const double *const *const *inputs,
                                                 uintptr_t num_assets,
                                                 uintptr_t data_len,
                                                 const double *options,
                                                 const bool *optional_outputs,
                                                 uintptr_t num_optional);

/**
 * Computes Chandelier Exit for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`),
 * each `data_len` `f64`s long, shared across all option sets.
 * `options` must point to `num_option_sets` pointers, each itself
 * pointing to `OPTIONS` (2) values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `chandelierexit` continuation state
 * (reusable with `chandelierexit_batch()`/`chandelierexit_state_free()`). Free each state via
 * `chandelierexit_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `num_optional` valid
 *   `bool`s.
 */
struct CSimdResult chandelierexit_simd_by_options(const double *const *inputs,
                                                  uintptr_t data_len,
                                                  const double *const *options,
                                                  uintptr_t num_option_sets,
                                                  const bool *optional_outputs,
                                                  uintptr_t num_optional);

/**
 * Returns static metadata about the `cmo` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Cmo::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo cmo_info(void);

/**
 * Returns the minimum number of bars `cmo` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t cmo_min_data(const double *options);

/**
 * Runs `cmo` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `cmo` output plus any requested optional outputs
 * and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult cmo_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues a `cmo` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `cmo_indicator()` and not yet have been passed to `cmo_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `cmo_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult cmo_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `cmo_indicator()` (or one of the
 * `states[i]` entries from `cmo_simd_by_assets()`/`cmo_simd_by_options()`).
 * Call this once you're done streaming (after your last `cmo_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `cmo_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void cmo_state_free(void *state);

/**
 * Computes CMO for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `cmo` continuation state (reusable
 * with `cmo_batch()`/`cmo_state_free()`). Free each state via
 * `cmo_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult cmo_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Computes CMO for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `cmo` continuation state
 * (reusable with `cmo_batch()`/`cmo_state_free()`). Free each state via
 * `cmo_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult cmo_simd_by_options(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *const *options,
                                       uintptr_t num_option_sets,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Frees a `CBytes` returned from `tulip_state_serialize`. Must be called
 * exactly once. Safe to call with null ptr (e.g. after an error).
 *
 * # Safety
 * `bytes` must be exactly what `tulip_state_serialize` returned, and must
 * not have been freed already.
 */
void tulip_ffi_bytes_free(struct CBytes bytes);

/**
 * Frees the output buffers owned by a `CIndicatorResult`. Does **not**
 * touch `state` -- call the indicator-specific `*_state_free()` for that.
 *
 * # Safety
 * Must be called at most once per `CIndicatorResult`, and only on a value
 * actually returned by one of this crate's `*_indicator()` functions.
 */
void tulip_ffi_result_free(struct CIndicatorResult result);

/**
 * Frees the output buffers owned by a `CBatchResult`.
 *
 * # Safety
 * Must be called at most once per `CBatchResult`, and only on a value
 * actually returned by one of this crate's `*_batch()` functions.
 */
void tulip_ffi_batch_result_free(struct CBatchResult result);

/**
 * Frees the output buffers and the states outer array wrapper owned by a `CSimdResult`.
 * Does NOT drop/free the individual boxed states pointed to by each `states[i]`
 * (the caller must call the indicator-specific `*_state_free()` on each one first).
 *
 * # Safety
 * Must be called at most once per `CSimdResult`, and only on a value actually
 * returned by one of this crate's `*_simd_by_assets()`/`*_simd_by_options()` functions.
 * The individual state pointers in `states[i]` must still be valid (not freed already)
 * when calling this function; they are not dropped here.
 */
void tulip_ffi_simd_result_free(struct CSimdResult result);

/**
 * Runs `cvi` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (2) pointers, in order:
 * `high, low`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `cvi` output plus any requested optional outputs
 * and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `num_optional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult cvi_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t num_optional);

/**
 * Continues a `cvi` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `cvi_indicator()` and not yet have been passed to `cvi_state_free()`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`high, low`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `cvi_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `num_optional` valid
 *   `bool`s.
 */
struct CBatchResult cvi_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t num_optional);

/**
 * Frees a state handle returned by `cvi_indicator()` (or one of the
 * `states[i]` entries from `cvi_simd_by_assets()`/`cvi_simd_by_options()`).
 * Call this once you're done streaming (after your last `cvi_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `cvi_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void cvi_state_free(void *state);

/**
 * Computes CVI for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (2) pointers (`high, low`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `cvi` continuation state (reusable
 * with `cvi_batch()`/`cvi_state_free()`). Free each state via
 * `cvi_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `num_optional` valid
 *   `bool`s.
 */
struct CSimdResult cvi_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t num_optional);

/**
 * Computes CVI for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`high, low`),
 * each `data_len` `f64`s long, shared across all option sets.
 * `options` must point to `num_option_sets` pointers, each itself
 * pointing to `OPTIONS` (1) values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `cvi` continuation state
 * (reusable with `cvi_batch()`/`cvi_state_free()`). Free each state via
 * `cvi_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `num_optional` valid
 *   `bool`s.
 */
struct CSimdResult cvi_simd_by_options(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *const *options,
                                       uintptr_t num_option_sets,
                                       const bool *optional_outputs,
                                       uintptr_t num_optional);

/**
 * Returns static metadata about the `cybercycle` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Cybercycle::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo cybercycle_info(void);

/**
 * Returns the minimum number of bars `cybercycle` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t cybercycle_min_data(const double *options);

/**
 * Runs `cybercycle` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `alpha`.
 *
 * Returns the mandatory `cybercycle`/`trigger` outputs plus any requested optional
 * outputs and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult cybercycle_indicator(const double *const *inputs,
                                             uintptr_t data_len,
                                             const double *options,
                                             const bool *optional_outputs,
                                             uintptr_t numoptional);

/**
 * Continues a `cybercycle` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `cybercycle_indicator()` and not yet have been passed to `cybercycle_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `cybercycle_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult cybercycle_batch(void *state,
                                     const double *const *inputs,
                                     uintptr_t data_len,
                                     const bool *optional_outputs,
                                     uintptr_t numoptional);

/**
 * Frees a state handle returned by `cybercycle_indicator()` (or one of the
 * `states[i]` entries from `cybercycle_simd_by_assets()`/`cybercycle_simd_by_options()`).
 * Call this once you're done streaming (after your last `cybercycle_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `cybercycle_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void cybercycle_state_free(void *state);

/**
 * Computes CyberCycle for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `cybercycle` continuation state (reusable
 * with `cybercycle_batch()`/`cybercycle_state_free()`). Free each state via
 * `cybercycle_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult cybercycle_simd_by_assets(const double *const *const *inputs,
                                             uintptr_t num_assets,
                                             uintptr_t data_len,
                                             const double *options,
                                             const bool *optional_outputs,
                                             uintptr_t numoptional);

/**
 * Computes CyberCycle for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `cybercycle` continuation state
 * (reusable with `cybercycle_batch()`/`cybercycle_state_free()`). Free each state via
 * `cybercycle_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult cybercycle_simd_by_options(const double *const *inputs,
                                              uintptr_t data_len,
                                              const double *const *options,
                                              uintptr_t num_option_sets,
                                              const bool *optional_outputs,
                                              uintptr_t numoptional);

/**
 * Returns static metadata about the `dema` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Dema::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo dema_info(void);

/**
 * Returns the minimum number of bars `dema` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t dema_min_data(const double *options);

/**
 * Runs `dema` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `dema`/`ema` outputs plus any requested optional
 * outputs and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult dema_indicator(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Continues a `dema` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `dema_indicator()` and not yet have been passed to `dema_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `dema_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult dema_batch(void *state,
                               const double *const *inputs,
                               uintptr_t data_len,
                               const bool *optional_outputs,
                               uintptr_t numoptional);

/**
 * Frees a state handle returned by `dema_indicator()` (or one of the
 * `states[i]` entries from `dema_simd_by_assets()`/`dema_simd_by_options()`).
 * Call this once you're done streaming (after your last `dema_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `dema_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void dema_state_free(void *state);

/**
 * Computes DEMA for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `dema` continuation state (reusable
 * with `dema_batch()`/`dema_state_free()`). Free each state via
 * `dema_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult dema_simd_by_assets(const double *const *const *inputs,
                                       uintptr_t num_assets,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Computes DEMA for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `dema` continuation state
 * (reusable with `dema_batch()`/`dema_state_free()`). Free each state via
 * `dema_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult dema_simd_by_options(const double *const *inputs,
                                        uintptr_t data_len,
                                        const double *const *options,
                                        uintptr_t num_option_sets,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Returns static metadata about the `di` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Di::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo di_info(void);

/**
 * Returns the minimum number of bars `di` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t di_min_data(const double *options);

/**
 * Runs `di` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers, in order:
 * `high, low, close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `plus_di`/`minus_di` outputs plus any requested
 * optional outputs (`atr`, `tr`, in that fixed order) and a fresh
 * continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult di_indicator(const double *const *inputs,
                                     uintptr_t data_len,
                                     const double *options,
                                     const bool *optional_outputs,
                                     uintptr_t numoptional);

/**
 * Continues a `di` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `di_indicator()` and not yet have been passed to `di_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`), each
 * `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `di_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult di_batch(void *state,
                             const double *const *inputs,
                             uintptr_t data_len,
                             const bool *optional_outputs,
                             uintptr_t numoptional);

/**
 * Frees a state handle returned by `di_indicator()` (or one of the
 * `states[i]` entries from `di_simd_by_assets()`/`di_simd_by_options()`).
 * Call this once you're done streaming (after your last `di_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `di_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void di_state_free(void *state);

/**
 * Computes DI for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) shared value.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `di` continuation state (reusable
 * with `di_batch()`/`di_state_free()`). Free each state via
 * `di_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult di_simd_by_assets(const double *const *const *inputs,
                                     uintptr_t num_assets,
                                     uintptr_t data_len,
                                     const double *options,
                                     const bool *optional_outputs,
                                     uintptr_t numoptional);

/**
 * Computes DI for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`), each
 * `data_len` `f64`s long, shared across all option sets. `options` must point
 * to `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * value.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `di` continuation state
 * (reusable with `di_batch()`/`di_state_free()`). Free each state via
 * `di_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult di_simd_by_options(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *const *options,
                                      uintptr_t num_option_sets,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Returns static metadata about the `dm` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Dm::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo dm_info(void);

/**
 * Returns the minimum number of bars `dm` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t dm_min_data(const double *options);

/**
 * Runs `dm` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (2) pointers, in order:
 * `high, low`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `plus_dm`/`minus_dm` outputs. DM has no optional
 * outputs.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult dm_indicator(const double *const *inputs,
                                     uintptr_t data_len,
                                     const double *options,
                                     const bool *optional_outputs,
                                     uintptr_t numoptional);

/**
 * Continues a `dm` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `dm_indicator()` and not yet have been passed to `dm_state_free()`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`high, low`), each
 * `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `dm_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult dm_batch(void *state,
                             const double *const *inputs,
                             uintptr_t data_len,
                             const bool *optional_outputs,
                             uintptr_t numoptional);

/**
 * Frees a state handle returned by `dm_indicator()` (or one of the
 * `states[i]` entries from `dm_simd_by_assets()`/`dm_simd_by_options()`).
 * Call this once you're done streaming (after your last `dm_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `dm_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void dm_state_free(void *state);

/**
 * Computes DM for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (2) pointers (`high, low`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) shared value.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `dm` continuation state (reusable
 * with `dm_batch()`/`dm_state_free()`). Free each state via
 * `dm_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult dm_simd_by_assets(const double *const *const *inputs,
                                     uintptr_t num_assets,
                                     uintptr_t data_len,
                                     const double *options,
                                     const bool *optional_outputs,
                                     uintptr_t numoptional);

/**
 * Computes DM for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`high, low`), each
 * `data_len` `f64`s long, shared across all option sets. `options` must point
 * to `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * value.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `dm` continuation state
 * (reusable with `dm_batch()`/`dm_state_free()`). Free each state via
 * `dm_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult dm_simd_by_options(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *const *options,
                                      uintptr_t num_option_sets,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Returns static metadata about the `donchianchannel` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `DonchianChannel::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo donchianchannel_info(void);

/**
 * Returns the minimum number of bars `donchianchannel` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t donchianchannel_min_data(const double *options);

/**
 * Runs `donchianchannel` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (2) pointers, in order:
 * `high, low`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `lower`/`middle`/`upper` outputs. Donchian Channel has no optional
 * outputs.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult donchianchannel_indicator(const double *const *inputs,
                                                  uintptr_t data_len,
                                                  const double *options,
                                                  const bool *optional_outputs,
                                                  uintptr_t numoptional);

/**
 * Continues a `donchianchannel` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `donchianchannel_indicator()` and not yet have been passed to `donchianchannel_state_free()`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`high, low`), each
 * `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `donchianchannel_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult donchianchannel_batch(void *state,
                                          const double *const *inputs,
                                          uintptr_t data_len,
                                          const bool *optional_outputs,
                                          uintptr_t numoptional);

/**
 * Frees a state handle returned by `donchianchannel_indicator()` (or one of the
 * `states[i]` entries from `donchianchannel_simd_by_assets()`/`donchianchannel_simd_by_options()`).
 * Call this once you're done streaming (after your last `donchianchannel_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `donchianchannel_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void donchianchannel_state_free(void *state);

/**
 * Computes Donchian Channel for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (2) pointers (`high, low`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) shared value.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `donchianchannel` continuation state (reusable
 * with `donchianchannel_batch()`/`donchianchannel_state_free()`). Free each state via
 * `donchianchannel_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult donchianchannel_simd_by_assets(const double *const *const *inputs,
                                                  uintptr_t num_assets,
                                                  uintptr_t data_len,
                                                  const double *options,
                                                  const bool *optional_outputs,
                                                  uintptr_t numoptional);

/**
 * Computes Donchian Channel for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`high, low`), each
 * `data_len` `f64`s long, shared across all option sets. `options` must point
 * to `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * value.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `donchianchannel` continuation state
 * (reusable with `donchianchannel_batch()`/`donchianchannel_state_free()`). Free each state via
 * `donchianchannel_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult donchianchannel_simd_by_options(const double *const *inputs,
                                                   uintptr_t data_len,
                                                   const double *const *options,
                                                   uintptr_t num_option_sets,
                                                   const bool *optional_outputs,
                                                   uintptr_t numoptional);

/**
 * Returns static metadata about the `dpo` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Dpo::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo dpo_info(void);

/**
 * Returns the minimum number of bars `dpo` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t dpo_min_data(const double *options);

/**
 * Runs `dpo` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `dpo` output plus any requested optional outputs
 * (`sma`, in that fixed order) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult dpo_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues a `dpo` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `dpo_indicator()` and not yet have been passed to `dpo_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `dpo_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult dpo_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `dpo_indicator()` (or one of the
 * `states[i]` entries from `dpo_simd_by_assets()`/`dpo_simd_by_options()`).
 * Call this once you're done streaming (after your last `dpo_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `dpo_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void dpo_state_free(void *state);

/**
 * Computes DPO for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (1) shared value.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `dpo` continuation state (reusable
 * with `dpo_batch()`/`dpo_state_free()`). Free each state via
 * `dpo_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult dpo_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Computes DPO for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * value.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `dpo` continuation state
 * (reusable with `dpo_batch()`/`dpo_state_free()`). Free each state via
 * `dpo_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult dpo_simd_by_options(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *const *options,
                                       uintptr_t num_option_sets,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Returns static metadata about the `dx` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Dx::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo dx_info(void);

/**
 * Returns the minimum number of bars `dx` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t dx_min_data(const double *options);

/**
 * Runs `dx` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers, in order:
 * `high, low, close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `dx` output plus any requested optional outputs
 * (`atr`, `tr`, in that fixed order) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult dx_indicator(const double *const *inputs,
                                     uintptr_t data_len,
                                     const double *options,
                                     const bool *optional_outputs,
                                     uintptr_t numoptional);

/**
 * Continues a `dx` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `dx_indicator()` and not yet have been passed to `dx_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`), each
 * `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `dx_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult dx_batch(void *state,
                             const double *const *inputs,
                             uintptr_t data_len,
                             const bool *optional_outputs,
                             uintptr_t numoptional);

/**
 * Frees a state handle returned by `dx_indicator()` (or one of the
 * `states[i]` entries from `dx_simd_by_assets()`/`dx_simd_by_options()`).
 * Call this once you're done streaming (after your last `dx_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `dx_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void dx_state_free(void *state);

/**
 * Computes DX for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) shared value.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `dx` continuation state (reusable
 * with `dx_batch()`/`dx_state_free()`). Free each state via
 * `dx_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult dx_simd_by_assets(const double *const *const *inputs,
                                     uintptr_t num_assets,
                                     uintptr_t data_len,
                                     const double *options,
                                     const bool *optional_outputs,
                                     uintptr_t numoptional);

/**
 * Computes DX for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`), each
 * `data_len` `f64`s long, shared across all option sets. `options` must point
 * to `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * value.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `dx` continuation state
 * (reusable with `dx_batch()`/`dx_state_free()`). Free each state via
 * `dx_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult dx_simd_by_options(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *const *options,
                                      uintptr_t num_option_sets,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Returns static metadata about the `ef` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Ef::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo ef_info(void);

/**
 * Returns the minimum number of bars `ef` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t ef_min_data(const double *options);

/**
 * Runs `ef` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `ef` output plus any requested optional outputs
 * (none for ef) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult ef_indicator(const double *const *inputs,
                                     uintptr_t data_len,
                                     const double *options,
                                     const bool *optional_outputs,
                                     uintptr_t numoptional);

/**
 * Continues an `ef` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `ef_indicator()` and not yet have been passed to `ef_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `ef_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult ef_batch(void *state,
                             const double *const *inputs,
                             uintptr_t data_len,
                             const bool *optional_outputs,
                             uintptr_t numoptional);

/**
 * Frees a state handle returned by `ef_indicator()` (or one of the
 * `states[i]` entries from `ef_simd_by_assets()`/`ef_simd_by_options()`).
 * Call this once you're done streaming (after your last `ef_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `ef_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void ef_state_free(void *state);

/**
 * Computes EF for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `ef` continuation state (reusable
 * with `ef_batch()`/`ef_state_free()`). Free each state via
 * `ef_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult ef_simd_by_assets(const double *const *const *inputs,
                                     uintptr_t num_assets,
                                     uintptr_t data_len,
                                     const double *options,
                                     const bool *optional_outputs,
                                     uintptr_t numoptional);

/**
 * Computes EF for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `ef` continuation state
 * (reusable with `ef_batch()`/`ef_state_free()`). Free each state via
 * `ef_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult ef_simd_by_options(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *const *options,
                                      uintptr_t num_option_sets,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Returns static metadata about the `elderray` indicator: its name, input
 * * names, option names, and (mandatory/optional) output names, mirroring
 * * `Elderray::INFO`.
 * *
 * * The returned strings are leaked, process-lifetime C strings -- read them,
 * * don't free them.
 */
struct CIndicatorInfo elderray_info(void);

/**
 * Returns the minimum number of bars `elderray` needs to produce any output at
 * * all, given `options`.
 * *
 * * # Safety
 * * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t elderray_min_data(const double *options);

/**
 * Runs `elderray` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers, in order:
 * `high, low, close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `elderray` outputs (`bull`, `bear`) plus any requested
 * optional outputs (`ema`, in that fixed order) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult elderray_indicator(const double *const *inputs,
                                           uintptr_t data_len,
                                           const double *options,
                                           const bool *optional_outputs,
                                           uintptr_t numoptional);

/**
 * Continues an `elderray` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `elderray_indicator()` and not yet have been passed to `elderray_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `elderray_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult elderray_batch(void *state,
                                   const double *const *inputs,
                                   uintptr_t data_len,
                                   const bool *optional_outputs,
                                   uintptr_t numoptional);

/**
 * Frees a state handle returned by `elderray_indicator()` (or one of the
 * `states[i]` entries from `elderray_simd_by_assets()`/`elderray_simd_by_options()`).
 * Call this once you're done streaming (after your last `elderray_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `elderray_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void elderray_state_free(void *state);

/**
 * Computes Elderray for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s
 * long. `options` must point to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `elderray` continuation state (reusable
 * with `elderray_batch()`/`elderray_state_free()`). Free each state via
 * `elderray_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult elderray_simd_by_assets(const double *const *const *inputs,
                                           uintptr_t num_assets,
                                           uintptr_t data_len,
                                           const double *options,
                                           const bool *optional_outputs,
                                           uintptr_t numoptional);

/**
 * Computes Elderray for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`),
 * each `data_len` `f64`s long, shared across all option sets.
 * `options` must point to `num_option_sets` pointers, each itself
 * pointing to `OPTIONS` (1) values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `elderray` continuation state
 * (reusable with `elderray_batch()`/`elderray_state_free()`). Free each state
 * via `elderray_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult elderray_simd_by_options(const double *const *inputs,
                                            uintptr_t data_len,
                                            const double *const *options,
                                            uintptr_t num_option_sets,
                                            const bool *optional_outputs,
                                            uintptr_t numoptional);

/**
 * Returns static metadata about the `ema` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Ema::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo ema_info(void);

/**
 * Returns the minimum number of bars `ema` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t ema_min_data(const double *options);

/**
 * Runs `ema` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `ema` output plus any requested optional outputs
 * (none for ema) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult ema_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues an `ema` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `ema_indicator()` and not yet have been passed to `ema_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `ema_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult ema_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `ema_indicator()` (or one of the
 * `states[i]` entries from `ema_simd_by_assets()`/`ema_simd_by_options()`).
 * Call this once you're done streaming (after your last `ema_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `ema_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void ema_state_free(void *state);

/**
 * Computes EMA for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `ema` continuation state (reusable
 * with `ema_batch()`/`ema_state_free()`). Free each state via
 * `ema_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult ema_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Computes EMA for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `ema` continuation state
 * (reusable with `ema_batch()`/`ema_state_free()`). Free each state via
 * `ema_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult ema_simd_by_options(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *const *options,
                                       uintptr_t num_option_sets,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Returns static metadata about the `emv` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Emv::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo emv_info(void);

/**
 * Returns the minimum number of bars `emv` needs to produce any output at
 * all, given `options`.
 *
 * Note: EMV has no options (`OPTIONS == 0`).
 */
uintptr_t emv_min_data(const double *_options);

/**
 * Runs `emv` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers, in order:
 * `high, low, volume`, each `data_len` `f64`s long.
 * `options` is ignored (EMV has no options).
 *
 * Returns the mandatory `emv` output plus any requested optional outputs
 * (`medprice`, in that fixed order) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s (but OPTIONS is 0 for EMV).
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult emv_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues an `emv` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `emv_indicator()` and not yet have been passed to `emv_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, volume`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `emv_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult emv_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `emv_indicator()` (or one of the
 * `states[i]` entries from `emv_simd_by_assets()`/`emv_simd_by_options()`).
 * Call this once you're done streaming (after your last `emv_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `emv_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void emv_state_free(void *state);

/**
 * Computes EMV for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high, low, volume`), each `data_len` `f64`s
 * long. `options` is ignored (EMV has no options).
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `emv` continuation state (reusable
 * with `emv_batch()`/`emv_state_free()`). Free each state via
 * `emv_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s (but OPTIONS is 0 for EMV).
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult emv_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 *  Returns static metadata about the `fisher` indicator: its name, input
 * * names, option names, and (mandatory/optional) output names, mirroring
 * * `Fisher::INFO`.
 * *
 * * The returned strings are leaked, process-lifetime C strings -- read them,
 * * don't free them.
 */
struct CIndicatorInfo fisher_info(void);

/**
 *  Returns the minimum number of bars `fisher` needs to produce any output at
 * * all, given `options`.
 * *
 * * # Safety
 * * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t fisher_min_data(const double *options);

/**
 * Runs `fisher` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (2) pointers, in order:
 * `high, low`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `fisher` outputs (`fisher`, `fisher_signal`) plus any
 * requested optional outputs (none for fisher) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult fisher_indicator(const double *const *inputs,
                                         uintptr_t data_len,
                                         const double *options,
                                         const bool *optional_outputs,
                                         uintptr_t numoptional);

/**
 * Continues a `fisher` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `fisher_indicator()` and not yet have been passed to `fisher_state_free()`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`high, low`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `fisher_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult fisher_batch(void *state,
                                 const double *const *inputs,
                                 uintptr_t data_len,
                                 const bool *optional_outputs,
                                 uintptr_t numoptional);

/**
 * Frees a state handle returned by `fisher_indicator()` (or one of the
 * `states[i]` entries from `fisher_simd_by_assets()`/`fisher_simd_by_options()`).
 * Call this once you're done streaming (after your last `fisher_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `fisher_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void fisher_state_free(void *state);

/**
 * Computes Fisher Transform for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (2) pointers (`high, low`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `fisher` continuation state (reusable
 * with `fisher_batch()`/`fisher_state_free()`). Free each state via
 * `fisher_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult fisher_simd_by_assets(const double *const *const *inputs,
                                         uintptr_t num_assets,
                                         uintptr_t data_len,
                                         const double *options,
                                         const bool *optional_outputs,
                                         uintptr_t numoptional);

/**
 * Computes Fisher Transform for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`high, low`),
 * each `data_len` `f64`s long, shared across all option sets.
 * `options` must point to `num_option_sets` pointers, each itself
 * pointing to `OPTIONS` (1) values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `fisher` continuation state
 * (reusable with `fisher_batch()`/`fisher_state_free()`). Free each state via
 * `fisher_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult fisher_simd_by_options(const double *const *inputs,
                                          uintptr_t data_len,
                                          const double *const *options,
                                          uintptr_t num_option_sets,
                                          const bool *optional_outputs,
                                          uintptr_t numoptional);

/**
 * Returns static metadata about the `fosc` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Fosc::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo fosc_info(void);

/**
 * Returns the minimum number of bars `fosc` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t fosc_min_data(const double *options);

/**
 * Runs `fosc` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `fosc` output plus any requested optional outputs
 * (`tsf`, `linreg`, `linregslope`, `linregintercept`, in that fixed order)
 * and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult fosc_indicator(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Continues a `fosc` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `fosc_indicator()` and not yet have been passed to `fosc_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `fosc_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult fosc_batch(void *state,
                               const double *const *inputs,
                               uintptr_t data_len,
                               const bool *optional_outputs,
                               uintptr_t numoptional);

/**
 * Frees a state handle returned by `fosc_indicator()` (or one of the
 * `states[i]` entries from `fosc_simd_by_assets()`/`fosc_simd_by_options()`).
 * Call this once you're done streaming (after your last `fosc_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `fosc_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void fosc_state_free(void *state);

/**
 * Computes FOSC for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `fosc` continuation state (reusable
 * with `fosc_batch()`/`fosc_state_free()`). Free each state via
 * `fosc_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult fosc_simd_by_assets(const double *const *const *inputs,
                                       uintptr_t num_assets,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Computes FOSC for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `fosc` continuation state
 * (reusable with `fosc_batch()`/`fosc_state_free()`). Free each state
 * via `fosc_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult fosc_simd_by_options(const double *const *inputs,
                                        uintptr_t data_len,
                                        const double *const *options,
                                        uintptr_t num_option_sets,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Returns static metadata about the `highpass` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `HighPass::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo highpass_info(void);

/**
 * Returns the minimum number of bars `highpass` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t highpass_min_data(const double *options);

/**
 * Runs `highpass` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `highpass` output only (no optional outputs).
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs` is ignored (highpass has no optional outputs).
 */
struct CIndicatorResult highpass_indicator(const double *const *inputs,
                                           uintptr_t data_len,
                                           const double *options,
                                           const bool *optional_outputs,
                                           uintptr_t numoptional);

/**
 * Continues a `highpass` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `highpass_indicator()` and not yet have been passed to `highpass_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `highpass_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs` is ignored (highpass has no optional outputs).
 */
struct CBatchResult highpass_batch(void *state,
                                   const double *const *inputs,
                                   uintptr_t data_len,
                                   const bool *optional_outputs,
                                   uintptr_t numoptional);

/**
 * Frees a state handle returned by `highpass_indicator()` (or one of the
 * `states[i]` entries from `highpass_simd_by_assets()`/`highpass_simd_by_options()`).
 * Call this once you're done streaming (after your last `highpass_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `highpass_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void highpass_state_free(void *state);

/**
 * Computes HighPass for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `highpass` continuation state (reusable
 * with `highpass_batch()`/`highpass_state_free()`). Free each state via
 * `highpass_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs` is ignored (highpass has no optional outputs).
 */
struct CSimdResult highpass_simd_by_assets(const double *const *const *inputs,
                                           uintptr_t num_assets,
                                           uintptr_t data_len,
                                           const double *options,
                                           const bool *optional_outputs,
                                           uintptr_t numoptional);

/**
 * Computes HighPass for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `highpass` continuation state
 * (reusable with `highpass_batch()`/`highpass_state_free()`). Free each state
 * via `highpass_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs` is ignored (highpass has no optional outputs).
 */
struct CSimdResult highpass_simd_by_options(const double *const *inputs,
                                            uintptr_t data_len,
                                            const double *const *options,
                                            uintptr_t num_option_sets,
                                            const bool *optional_outputs,
                                            uintptr_t numoptional);

/**
 * Returns static metadata about the `hilberttransform` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `HilbertTransform::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo hilberttransform_info(void);

/**
 * Returns the minimum number of bars `hilberttransform` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (2) valid `f64`s.
 */
uintptr_t hilberttransform_min_data(const double *options);

/**
 * Runs `hilberttransform` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (2) values: `ss_period, hp_period`.
 *
 * Returns the mandatory outputs (`in_phase`, `quadrature`) plus any requested
 * optional outputs (`roofing`, `highpass`, in that fixed order).
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult hilberttransform_indicator(const double *const *inputs,
                                                   uintptr_t data_len,
                                                   const double *options,
                                                   const bool *optional_outputs,
                                                   uintptr_t numoptional);

/**
 * Continues a `hilberttransform` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `hilberttransform_indicator()` and not yet have been passed to `hilberttransform_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `hilberttransform_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult hilberttransform_batch(void *state,
                                           const double *const *inputs,
                                           uintptr_t data_len,
                                           const bool *optional_outputs,
                                           uintptr_t numoptional);

/**
 * Frees a state handle returned by `hilberttransform_indicator()` (or one of the
 * `states[i]` entries from `hilberttransform_simd_by_assets()`/`hilberttransform_simd_by_options()`).
 * Call this once you're done streaming (after your last `hilberttransform_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `hilberttransform_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void hilberttransform_state_free(void *state);

/**
 * Computes HilbertTransform for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (2) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `hilberttransform` continuation state (reusable
 * with `hilberttransform_batch()`/`hilberttransform_state_free()`). Free each state via
 * `hilberttransform_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult hilberttransform_simd_by_assets(const double *const *const *inputs,
                                                   uintptr_t num_assets,
                                                   uintptr_t data_len,
                                                   const double *options,
                                                   const bool *optional_outputs,
                                                   uintptr_t numoptional);

/**
 * Computes HilbertTransform for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (2)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `hilberttransform` continuation state
 * (reusable with `hilberttransform_batch()`/`hilberttransform_state_free()`). Free each state
 * via `hilberttransform_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult hilberttransform_simd_by_options(const double *const *inputs,
                                                    uintptr_t data_len,
                                                    const double *const *options,
                                                    uintptr_t num_option_sets,
                                                    const bool *optional_outputs,
                                                    uintptr_t numoptional);

/**
 * Returns static metadata about the `hma` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Hma::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo hma_info(void);

/**
 * Returns the minimum number of bars `hma` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t hma_min_data(const double *options);

/**
 * Runs `hma` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `hma` output only (no optional outputs).
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs` is ignored (hma has no optional outputs).
 */
struct CIndicatorResult hma_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues a `hma` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `hma_indicator()` and not yet have been passed to `hma_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `hma_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs` is ignored (hma has no optional outputs).
 */
struct CBatchResult hma_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `hma_indicator()` (or one of the
 * `states[i]` entries from `hma_simd_by_assets()`/`hma_simd_by_options()`).
 * Call this once you're done streaming (after your last `hma_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `hma_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void hma_state_free(void *state);

/**
 * Computes HMA for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `hma` continuation state (reusable
 * with `hma_batch()`/`hma_state_free()`). Free each state via
 * `hma_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs` is ignored (hma has no optional outputs).
 */
struct CSimdResult hma_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Computes HMA for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `hma` continuation state
 * (reusable with `hma_batch()`/`hma_state_free()`). Free each state
 * via `hma_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs` is ignored (hma has no optional outputs).
 */
struct CSimdResult hma_simd_by_options(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *const *options,
                                       uintptr_t num_option_sets,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Returns static metadata about the `homodynediscriminator` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `HomodyneDiscriminator::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo homodynediscriminator_info(void);

/**
 * Returns the minimum number of bars `homodynediscriminator` needs to produce any output at
 * all. This indicator has no options, so it always returns the same value.
 */
uintptr_t homodynediscriminator_min_data(const double *_options);

/**
 * Runs `homodynediscriminator` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` is ignored (the HD has no user parameters).
 *
 * Returns the mandatory `dc_period` output only (no optional outputs).
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` is ignored (homodynediscriminator has no options).
 * - `optional_outputs` is ignored (homodynediscriminator has no optional outputs).
 */
struct CIndicatorResult homodynediscriminator_indicator(const double *const *inputs,
                                                        uintptr_t data_len,
                                                        const double *_options,
                                                        const bool *optional_outputs,
                                                        uintptr_t numoptional);

/**
 * Continues a `homodynediscriminator` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `homodynediscriminator_indicator()` and not yet have been passed to `homodynediscriminator_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `homodynediscriminator_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs` is ignored (homodynediscriminator has no optional outputs).
 */
struct CBatchResult homodynediscriminator_batch(void *state,
                                                const double *const *inputs,
                                                uintptr_t data_len,
                                                const bool *optional_outputs,
                                                uintptr_t numoptional);

/**
 * Frees a state handle returned by `homodynediscriminator_indicator()` (or one of the
 * `states[i]` entries from `homodynediscriminator_simd_by_assets()`/`homodynediscriminator_simd_by_options()`).
 * Call this once you're done streaming (after your last `homodynediscriminator_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `homodynediscriminator_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void homodynediscriminator_state_free(void *state);

/**
 * Computes HomodyneDiscriminator for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` is ignored.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `homodynediscriminator` continuation state (reusable
 * with `homodynediscriminator_batch()`/`homodynediscriminator_state_free()`). Free each state via
 * `homodynediscriminator_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` is ignored (homodynediscriminator has no options).
 * - `optional_outputs` is ignored (homodynediscriminator has no optional outputs).
 */
struct CSimdResult homodynediscriminator_simd_by_assets(const double *const *const *inputs,
                                                        uintptr_t num_assets,
                                                        uintptr_t data_len,
                                                        const double *options,
                                                        const bool *optional_outputs,
                                                        uintptr_t numoptional);

/**
 * Returns static metadata about the `ichimoku` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Ichimoku::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo ichimoku_info(void);

/**
 * Returns the minimum number of bars `ichimoku` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (2) valid `f64`s.
 */
uintptr_t ichimoku_min_data(const double *options);

/**
 * Runs `ichimoku` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers, in order:
 * `high, low, close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (2) values: `short_period, long_period`.
 *
 * Returns the mandatory `ichimoku` outputs plus any requested optional outputs
 * (`lagging_span`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult ichimoku_indicator(const double *const *inputs,
                                           uintptr_t data_len,
                                           const double *options,
                                           const bool *optional_outputs,
                                           uintptr_t numoptional);

/**
 * Continues an `ichimoku` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `ichimoku_indicator()` and not yet have been passed to `ichimoku_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `ichimoku_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult ichimoku_batch(void *state,
                                   const double *const *inputs,
                                   uintptr_t data_len,
                                   const bool *optional_outputs,
                                   uintptr_t numoptional);

/**
 * Frees a state handle returned by `ichimoku_indicator()` (or one of the
 * `states[i]` entries from `ichimoku_simd_by_assets()`/`ichimoku_simd_by_options()`).
 * Call this once you're done streaming (after your last `ichimoku_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `ichimoku_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void ichimoku_state_free(void *state);

/**
 * Computes Ichimoku for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s
 * long. `options` must point to `OPTIONS` (2) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `ichimoku` continuation state (reusable
 * with `ichimoku_batch()`/`ichimoku_state_free()`). Free each state via
 * `ichimoku_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult ichimoku_simd_by_assets(const double *const *const *inputs,
                                           uintptr_t num_assets,
                                           uintptr_t data_len,
                                           const double *options,
                                           const bool *optional_outputs,
                                           uintptr_t numoptional);

/**
 * Computes Ichimoku for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`),
 * each `data_len` `f64`s long, shared across all option sets.
 * `options` must point to `num_option_sets` pointers, each itself
 * pointing to `OPTIONS` (2) values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `ichimoku` continuation state
 * (reusable with `ichimoku_batch()`/`ichimoku_state_free()`). Free each state via
 * `ichimoku_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult ichimoku_simd_by_options(const double *const *inputs,
                                            uintptr_t data_len,
                                            const double *const *options,
                                            uintptr_t num_option_sets,
                                            const bool *optional_outputs,
                                            uintptr_t numoptional);

/**
 * Returns static metadata about the `instantaneoustrendline` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `InstantaneousTrendline::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo instantaneoustrendline_info(void);

/**
 * Returns the minimum number of bars `instantaneoustrendline` needs to produce any output at
 * all, given `options`.
 */
uintptr_t instantaneoustrendline_min_data(const double *_options);

/**
 * Runs `instantaneoustrendline` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (0) values (empty array).
 *
 * Returns the mandatory `instantaneoustrendline` output plus any requested optional outputs
 * (`trigger, dc_period, alpha`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s (for OPTIONS=0, pass a null pointer or any pointer).
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult instantaneoustrendline_indicator(const double *const *inputs,
                                                         uintptr_t data_len,
                                                         const double *_options,
                                                         const bool *optional_outputs,
                                                         uintptr_t numoptional);

/**
 * Continues an `instantaneoustrendline` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `instantaneoustrendline_indicator()` and not yet have been passed to `instantaneoustrendline_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `instantaneoustrendline_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult instantaneoustrendline_batch(void *state,
                                                 const double *const *inputs,
                                                 uintptr_t data_len,
                                                 const bool *optional_outputs,
                                                 uintptr_t numoptional);

/**
 * Frees a state handle returned by `instantaneoustrendline_indicator()` (or one of the
 * `states[i]` entries from `instantaneoustrendline_simd_by_assets()`).
 * Call this once you're done streaming (after your last `instantaneoustrendline_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `instantaneoustrendline_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void instantaneoustrendline_state_free(void *state);

/**
 * Computes InstantaneousTrendline for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), each `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (0) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `instantaneoustrendline` continuation state (reusable
 * with `instantaneoustrendline_batch()`/`instantaneoustrendline_state_free()`). Free each state via
 * `instantaneoustrendline_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s (for OPTIONS=0, pass a null pointer or any pointer).
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult instantaneoustrendline_simd_by_assets(const double *const *const *inputs,
                                                         uintptr_t num_assets,
                                                         uintptr_t data_len,
                                                         const double *options,
                                                         const bool *optional_outputs,
                                                         uintptr_t numoptional);

/**
 * Returns static metadata about the `kama` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Kama::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo kama_info(void);

/**
 * Returns the minimum number of bars `kama` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t kama_min_data(const double *options);

/**
 * Runs `kama` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `kama` output plus any requested optional outputs
 * (`ef`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult kama_indicator(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Continues a `kama` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `kama_indicator()` and not yet have been passed to `kama_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `kama_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult kama_batch(void *state,
                               const double *const *inputs,
                               uintptr_t data_len,
                               const bool *optional_outputs,
                               uintptr_t numoptional);

/**
 * Frees a state handle returned by `kama_indicator()` (or one of the
 * `states[i]` entries from `kama_simd_by_assets()`/`kama_simd_by_options()`).
 * Call this once you're done streaming (after your last `kama_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `kama_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void kama_state_free(void *state);

/**
 * Computes KAMA for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), each `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `kama` continuation state (reusable
 * with `kama_batch()`/`kama_state_free()`). Free each state via
 * `kama_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult kama_simd_by_assets(const double *const *const *inputs,
                                       uintptr_t num_assets,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Computes KAMA for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `kama` continuation state
 * (reusable with `kama_batch()`/`kama_state_free()`). Free each state via
 * `kama_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult kama_simd_by_options(const double *const *inputs,
                                        uintptr_t data_len,
                                        const double *const *options,
                                        uintptr_t num_option_sets,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Returns static metadata about the `keltnerchannel` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `KeltnerChannel::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo keltnerchannel_info(void);

/**
 * Returns the minimum number of bars `keltnerchannel` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (2) valid `f64`s.
 */
uintptr_t keltnerchannel_min_data(const double *options);

/**
 * Runs `keltnerchannel` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers, in order:
 * `high, low, close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (2) values: `period, step`.
 *
 * Returns the mandatory `keltnerchannel` outputs plus any requested optional outputs
 * (`atr, tr`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult keltnerchannel_indicator(const double *const *inputs,
                                                 uintptr_t data_len,
                                                 const double *options,
                                                 const bool *optional_outputs,
                                                 uintptr_t numoptional);

/**
 * Continues a `keltnerchannel` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `keltnerchannel_indicator()` and not yet have been passed to `keltnerchannel_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `keltnerchannel_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult keltnerchannel_batch(void *state,
                                         const double *const *inputs,
                                         uintptr_t data_len,
                                         const bool *optional_outputs,
                                         uintptr_t numoptional);

/**
 * Frees a state handle returned by `keltnerchannel_indicator()` (or one of the
 * `states[i]` entries from `keltnerchannel_simd_by_assets()`/`keltnerchannel_simd_by_options()`).
 * Call this once you're done streaming (after your last `keltnerchannel_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `keltnerchannel_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void keltnerchannel_state_free(void *state);

/**
 * Computes KeltnerChannel for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s
 * long. `options` must point to `OPTIONS` (2) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `keltnerchannel` continuation state (reusable
 * with `keltnerchannel_batch()`/`keltnerchannel_state_free()`). Free each state via
 * `keltnerchannel_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult keltnerchannel_simd_by_assets(const double *const *const *inputs,
                                                 uintptr_t num_assets,
                                                 uintptr_t data_len,
                                                 const double *options,
                                                 const bool *optional_outputs,
                                                 uintptr_t numoptional);

/**
 * Computes KeltnerChannel for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`),
 * each `data_len` `f64`s long, shared across all option sets.
 * `options` must point to `num_option_sets` pointers, each itself
 * pointing to `OPTIONS` (2) values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `keltnerchannel` continuation state
 * (reusable with `keltnerchannel_batch()`/`keltnerchannel_state_free()`). Free each state via
 * `keltnerchannel_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult keltnerchannel_simd_by_options(const double *const *inputs,
                                                  uintptr_t data_len,
                                                  const double *const *options,
                                                  uintptr_t num_option_sets,
                                                  const bool *optional_outputs,
                                                  uintptr_t numoptional);

/**
 * Returns static metadata about the `kvo` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Kvo::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo kvo_info(void);

/**
 * Returns the minimum number of bars `kvo` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (2) valid `f64`s.
 */
uintptr_t kvo_min_data(const double *options);

/**
 * Runs `kvo` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (4) pointers, in order:
 * `high, low, close, volume`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (2) values: `short_period, long_period`.
 *
 * Returns the mandatory `kvo` output plus any requested optional outputs
 * (`short_ema, long_ema`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult kvo_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues a `kvo` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `kvo_indicator()` and not yet have been passed to `kvo_state_free()`.
 *
 * `inputs` must point to `INPUTS` (4) pointers (`high, low, close,
 * volume`), each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `kvo_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult kvo_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `kvo_indicator()` (or one of the
 * `states[i]` entries from `kvo_simd_by_assets()`/`kvo_simd_by_options()`).
 * Call this once you're done streaming (after your last `kvo_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `kvo_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void kvo_state_free(void *state);

/**
 * Computes KVO for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (4) pointers (`high, low, close, volume`), each `data_len` `f64`s
 * long. `options` must point to `OPTIONS` (2) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `kvo` continuation state (reusable
 * with `kvo_batch()`/`kvo_state_free()`). Free each state via
 * `kvo_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult kvo_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Computes KVO for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (4) pointers (`high, low, close,
 * volume`), each `data_len` `f64`s long, shared across all option sets.
 * `options` must point to `num_option_sets` pointers, each itself
 * pointing to `OPTIONS` (2) values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `kvo` continuation state
 * (reusable with `kvo_batch()`/`kvo_state_free()`). Free each state via
 * `kvo_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult kvo_simd_by_options(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *const *options,
                                       uintptr_t num_option_sets,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Returns static metadata about the `linreg` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Linreg::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo linreg_info(void);

/**
 * Returns the minimum number of bars `linreg` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t linreg_min_data(const double *options);

/**
 * Runs `linreg` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `linreg` output plus any requested optional outputs
 * (`linregslope`, `linregintercept`, in that fixed order) and a fresh
 * continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult linreg_indicator(const double *const *inputs,
                                         uintptr_t data_len,
                                         const double *options,
                                         const bool *optional_outputs,
                                         uintptr_t numoptional);

/**
 * Continues a `linreg` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `linreg_indicator()` and not yet have been passed to `linreg_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `linreg_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult linreg_batch(void *state,
                                 const double *const *inputs,
                                 uintptr_t data_len,
                                 const bool *optional_outputs,
                                 uintptr_t numoptional);

/**
 * Frees a state handle returned by `linreg_indicator()` (or one of the
 * `states[i]` entries from `linreg_simd_by_assets()`/`linreg_simd_by_options()`).
 * Call this once you're done streaming (after your last `linreg_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `linreg_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void linreg_state_free(void *state);

/**
 * Computes LINREG for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (1) shared value.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `linreg` continuation state (reusable
 * with `linreg_batch()`/`linreg_state_free()`). Free each state via
 * `linreg_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult linreg_simd_by_assets(const double *const *const *inputs,
                                         uintptr_t num_assets,
                                         uintptr_t data_len,
                                         const double *options,
                                         const bool *optional_outputs,
                                         uintptr_t numoptional);

/**
 * Computes LINREG for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (1) value.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `linreg` continuation state
 * (reusable with `linreg_batch()`/`linreg_state_free()`). Free each state
 * via `linreg_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult linreg_simd_by_options(const double *const *inputs,
                                          uintptr_t data_len,
                                          const double *const *options,
                                          uintptr_t num_option_sets,
                                          const bool *optional_outputs,
                                          uintptr_t numoptional);

/**
 * Returns static metadata about the `macd` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Macd::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo macd_info(void);

/**
 * Returns the minimum number of bars `macd` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (3) valid `f64`s.
 */
uintptr_t macd_min_data(const double *options);

/**
 * Runs `macd` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (3) values: `short_period,
 * long_period, signal_period`.
 *
 * Returns the mandatory `macd_line`/`signal_line`/`histogram` outputs plus
 * any requested optional outputs (`short_ema`, `long_ema`, in that fixed
 * order) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult macd_indicator(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Continues a `macd` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `macd_indicator()` and not yet have been passed to `macd_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `macd_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult macd_batch(void *state,
                               const double *const *inputs,
                               uintptr_t data_len,
                               const bool *optional_outputs,
                               uintptr_t numoptional);

/**
 * Frees a state handle returned by `macd_indicator()` (or one of the
 * `states[i]` entries from `macd_simd_by_assets()`/`macd_simd_by_options()`).
 * Call this once you're done streaming (after your last `macd_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `macd_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void macd_state_free(void *state);

/**
 * Computes MACD for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (3) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `macd` continuation state (reusable
 * with `macd_batch()`/`macd_state_free()`). Free each state via
 * `macd_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult macd_simd_by_assets(const double *const *const *inputs,
                                       uintptr_t num_assets,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Computes MACD for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (3)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `macd` continuation state
 * (reusable with `macd_batch()`/`macd_state_free()`). Free each state via
 * `macd_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult macd_simd_by_options(const double *const *inputs,
                                        uintptr_t data_len,
                                        const double *const *options,
                                        uintptr_t num_option_sets,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Returns static metadata about the `mama` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Mama::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo mama_info(void);

/**
 * Returns the minimum number of bars `mama` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (2) valid `f64`s.
 */
uintptr_t mama_min_data(const double *options);

/**
 * Runs `mama` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (2) values: `fast_limit, slow_limit`.
 *
 * Returns the mandatory `mama`/`fama` outputs plus any requested optional outputs
 * (`dc_period`, `alpha`, in that fixed order) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult mama_indicator(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Continues a `mama` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `mama_indicator()` and not yet have been passed to `mama_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `mama_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult mama_batch(void *state,
                               const double *const *inputs,
                               uintptr_t data_len,
                               const bool *optional_outputs,
                               uintptr_t numoptional);

/**
 * Frees a state handle returned by `mama_indicator()` (or one of the
 * `states[i]` entries from `mama_simd_by_assets()`/`mama_simd_by_options()`).
 * Call this once you're done streaming (after your last `mama_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `mama_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void mama_state_free(void *state);

/**
 * Computes MAMA for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (2) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `mama` continuation state (reusable
 * with `mama_batch()`/`mama_state_free()`). Free each state via
 * `mama_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult mama_simd_by_assets(const double *const *const *inputs,
                                       uintptr_t num_assets,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Computes MAMA for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (2) values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `mama` continuation state
 * (reusable with `mama_batch()`/`mama_state_free()`). Free each state
 * via `mama_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult mama_simd_by_options(const double *const *inputs,
                                        uintptr_t data_len,
                                        const double *const *options,
                                        uintptr_t num_option_sets,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Returns static metadata about the `marketfi` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Marketfi::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo marketfi_info(void);

/**
 * Returns the minimum number of bars `marketfi` needs to produce any output at
 * all. This indicator has no options, so it always returns 1.
 */
uintptr_t marketfi_min_data(const double *_options);

/**
 * Runs `marketfi` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers, in order:
 * `high, low, volume`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (0) values (no options).
 *
 * Returns the mandatory `marketfi` output plus any requested optional outputs
 * (none for this indicator) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s (pass null or any pointer for OPTIONS=0).
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult marketfi_indicator(const double *const *inputs,
                                           uintptr_t data_len,
                                           const double *options,
                                           const bool *optional_outputs,
                                           uintptr_t numoptional);

/**
 * Continues a `marketfi` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `marketfi_indicator()` and not yet have been passed to `marketfi_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, volume`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `marketfi_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult marketfi_batch(void *state,
                                   const double *const *inputs,
                                   uintptr_t data_len,
                                   const bool *optional_outputs,
                                   uintptr_t numoptional);

/**
 * Frees a state handle returned by `marketfi_indicator()` (or one of the
 * `states[i]` entries from `marketfi_simd_by_assets()`).
 * Call this once you're done streaming (after your last `marketfi_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `marketfi_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void marketfi_state_free(void *state);

/**
 * Computes MARKETFI for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high, low, volume`), `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (0) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `marketfi` continuation state (reusable
 * with `marketfi_batch()`/`marketfi_state_free()`). Free each state via
 * `marketfi_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s (pass null or any pointer for OPTIONS=0).
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult marketfi_simd_by_assets(const double *const *const *inputs,
                                           uintptr_t num_assets,
                                           uintptr_t data_len,
                                           const double *options,
                                           const bool *optional_outputs,
                                           uintptr_t numoptional);

/**
 * Returns static metadata about the `mass` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Mass::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo mass_info(void);

/**
 * Returns the minimum number of bars `mass` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t mass_min_data(const double *options);

/**
 * Runs `mass` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (2) pointers, in order:
 * `high, low`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `mass` output plus any requested optional outputs
 * (none for this indicator) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult mass_indicator(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Continues a `mass` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `mass_indicator()` and not yet have been passed to `mass_state_free()`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`high, low`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `mass_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult mass_batch(void *state,
                               const double *const *inputs,
                               uintptr_t data_len,
                               const bool *optional_outputs,
                               uintptr_t numoptional);

/**
 * Frees a state handle returned by `mass_indicator()` (or one of the
 * `states[i]` entries from `mass_simd_by_assets()`/`mass_simd_by_options()`).
 * Call this once you're done streaming (after your last `mass_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `mass_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void mass_state_free(void *state);

/**
 * Computes MASS for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (2) pointers (`high, low`), `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) shared value.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `mass` continuation state (reusable
 * with `mass_batch()`/`mass_state_free()`). Free each state via
 * `mass_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult mass_simd_by_assets(const double *const *const *inputs,
                                       uintptr_t num_assets,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Computes MASS for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`high, low`),
 * each `data_len` `f64`s long, shared across all option sets.
 * `options` must point to `num_option_sets` pointers, each itself
 * pointing to `OPTIONS` (1) value.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `mass` continuation state
 * (reusable with `mass_batch()`/`mass_state_free()`). Free each state
 * via `mass_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult mass_simd_by_options(const double *const *inputs,
                                        uintptr_t data_len,
                                        const double *const *options,
                                        uintptr_t num_option_sets,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Returns static metadata about the `max` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Max::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo max_info(void);

/**
 * Returns the minimum number of bars `max` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t max_min_data(const double *options);

/**
 * Runs `max` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `max` output plus any requested optional outputs
 * (none for this indicator) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult max_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues a `max` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `max_indicator()` and not yet have been passed to `max_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `max_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult max_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `max_indicator()` (or one of the
 * `states[i]` entries from `max_simd_by_assets()`/`max_simd_by_options()`).
 * Call this once you're done streaming (after your last `max_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `max_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void max_state_free(void *state);

/**
 * Computes MAX for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (1) shared value.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `max` continuation state (reusable
 * with `max_batch()`/`max_state_free()`). Free each state via
 * `max_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult max_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Computes MAX for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (1) value.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `max` continuation state
 * (reusable with `max_batch()`/`max_state_free()`). Free each state
 * via `max_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult max_simd_by_options(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *const *options,
                                       uintptr_t num_option_sets,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Returns static metadata about the `md` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Md::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo md_info(void);

/**
 * Returns the minimum number of bars `md` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t md_min_data(const double *options);

/**
 * Runs `md` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `md` output plus any requested optional outputs
 * (`sma`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult md_indicator(const double *const *inputs,
                                     uintptr_t data_len,
                                     const double *options,
                                     const bool *optional_outputs,
                                     uintptr_t numoptional);

/**
 * Continues a `md` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `md_indicator()` and not yet have been passed to `md_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `md_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult md_batch(void *state,
                             const double *const *inputs,
                             uintptr_t data_len,
                             const bool *optional_outputs,
                             uintptr_t numoptional);

/**
 * Frees a state handle returned by `md_indicator()` (or one of the
 * `states[i]` entries from `md_simd_by_assets()`/`md_simd_by_options()`).
 * Call this once you're done streaming (after your last `md_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `md_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void md_state_free(void *state);

/**
 * Computes MD for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `md` continuation state (reusable
 * with `md_batch()`/`md_state_free()`). Free each state via
 * `md_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult md_simd_by_assets(const double *const *const *inputs,
                                     uintptr_t num_assets,
                                     uintptr_t data_len,
                                     const double *options,
                                     const bool *optional_outputs,
                                     uintptr_t numoptional);

/**
 * Computes MD for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `md` continuation state
 * (reusable with `md_batch()`/`md_state_free()`). Free each state via
 * `md_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult md_simd_by_options(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *const *options,
                                      uintptr_t num_option_sets,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Returns static metadata about the `medprice` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `MedPrice::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo medprice_info(void);

/**
 * Returns the minimum number of bars `medprice` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (0) valid `f64`s.
 */
uintptr_t medprice_min_data(const double *options);

/**
 * Runs `medprice` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (2) pointers, in order:
 * `high, low`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (0) values (pass null).
 *
 * Returns the mandatory `medprice` output plus any requested optional outputs
 * (none available) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s (or null if OPTIONS is 0).
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult medprice_indicator(const double *const *inputs,
                                           uintptr_t data_len,
                                           const double *options,
                                           const bool *optional_outputs,
                                           uintptr_t numoptional);

/**
 * Continues a `medprice` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `medprice_indicator()` and not yet have been passed to `medprice_state_free()`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`high, low`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `medprice_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult medprice_batch(void *state,
                                   const double *const *inputs,
                                   uintptr_t data_len,
                                   const bool *optional_outputs,
                                   uintptr_t numoptional);

/**
 * Frees a state handle returned by `medprice_indicator()` (or one of the
 * `states[i]` entries from `medprice_simd_by_assets()`/`medprice_simd_by_options()`).
 * Call this once you're done streaming (after your last `medprice_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `medprice_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void medprice_state_free(void *state);

/**
 * Computes MEDPRICE for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (2) pointers (`high, low`), each `data_len` `f64`s
 * long. `options` must point to `OPTIONS` (0) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `medprice` continuation state (reusable
 * with `medprice_batch()`/`medprice_state_free()`). Free each state via
 * `medprice_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s (or null if OPTIONS is 0).
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult medprice_simd_by_assets(const double *const *const *inputs,
                                           uintptr_t num_assets,
                                           uintptr_t data_len,
                                           const double *options,
                                           const bool *optional_outputs,
                                           uintptr_t numoptional);

/**
 * Returns static metadata about the `mfi` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Mfi::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo mfi_info(void);

/**
 * Returns the minimum number of bars `mfi` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t mfi_min_data(const double *options);

/**
 * Runs `mfi` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (4) pointers, in order:
 * `high, low, close, volume`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `mfi` output plus any requested optional outputs
 * (`typprice`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult mfi_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues an `mfi` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `mfi_indicator()` and not yet have been passed to `mfi_state_free()`.
 *
 * `inputs` must point to `INPUTS` (4) pointers (`high, low, close,
 * volume`), each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `mfi_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult mfi_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `mfi_indicator()` (or one of the
 * `states[i]` entries from `mfi_simd_by_assets()`/`mfi_simd_by_options()`).
 * Call this once you're done streaming (after your last `mfi_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `mfi_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void mfi_state_free(void *state);

/**
 * Computes MFI for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (4) pointers (`high, low, close, volume`), each `data_len` `f64`s
 * long. `options` must point to `OPTIONS` (1) shared value: `period`.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `mfi` continuation state (reusable
 * with `mfi_batch()`/`mfi_state_free()`). Free each state via
 * `mfi_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult mfi_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Computes MFI for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (4) pointers (`high, low, close,
 * volume`), each `data_len` `f64`s long, shared across all option sets.
 * `options` must point to `num_option_sets` pointers, each itself
 * pointing to `OPTIONS` (1) value: `period`.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `mfi` continuation state
 * (reusable with `mfi_batch()`/`mfi_state_free()`). Free each state
 * via `mfi_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult mfi_simd_by_options(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *const *options,
                                       uintptr_t num_option_sets,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Returns static metadata about the `min` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Min::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo min_info(void);

/**
 * Returns the minimum number of bars `min` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t min_min_data(const double *options);

/**
 * Runs `min` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `min` output (no optional outputs available).
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 */
struct CIndicatorResult min_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *_optional_outputs,
                                      uintptr_t _num_optional);

/**
 * Continues a `min` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `min_indicator()` and not yet have been passed to `min_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `min_indicator()`.
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CBatchResult min_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *_optional_outputs,
                              uintptr_t _num_optional);

/**
 * Frees a state handle returned by `min_indicator()` (or one of the
 * `states[i]` entries from `min_simd_by_assets()`/`min_simd_by_options()`).
 * Call this once you're done streaming (after your last `min_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `min_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void min_state_free(void *state);

/**
 * Computes MIN for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer: `real`, each `data_len` `f64`s long. `options`
 * must point to `OPTIONS` (1) shared value: `period`.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `min` continuation state (reusable
 * with `min_batch()`/`min_state_free()`). Free each state via
 * `min_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 */
struct CSimdResult min_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *_optional_outputs,
                                      uintptr_t _num_optional);

/**
 * Computes MIN for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, each `data_len` `f64`s long,
 * shared across all option sets. `options` must point to `num_option_sets`
 * pointers, each itself pointing to `OPTIONS` (1) value: `period`.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `min` continuation state
 * (reusable with `min_batch()`/`min_state_free()`). Free each state
 * via `min_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 */
struct CSimdResult min_simd_by_options(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *const *options,
                                       uintptr_t num_option_sets,
                                       const bool *_optional_outputs,
                                       uintptr_t _num_optional);

/**
 * Returns static metadata about the `mom` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Mom::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo mom_info(void);

/**
 * Returns the minimum number of bars `mom` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t mom_min_data(const double *options);

/**
 * Runs `mom` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer, in order:
 * `real`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `mom` output plus any requested optional outputs
 * and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult mom_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues a `mom` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `mom_indicator()` and not yet have been passed to `mom_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `mom_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult mom_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `mom_indicator()` (or one of the
 * `states[i]` entries from `mom_simd_by_assets()`/`mom_simd_by_options()`).
 * Call this once you're done streaming (after your last `mom_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `mom_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void mom_state_free(void *state);

/**
 * Computes MOM for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointers (`real`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) shared value.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `mom` continuation state (reusable
 * with `mom_batch()`/`mom_state_free()`). Free each state via
 * `mom_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult mom_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Computes MOM for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), each `data_len`
 * `f64`s long, shared across all option sets.
 * `options` must point to `num_option_sets` pointers, each itself
 * pointing to `OPTIONS` (1) value.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `mom` continuation state
 * (reusable with `mom_batch()`/`mom_state_free()`). Free each state
 * via `mom_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult mom_simd_by_options(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *const *options,
                                       uintptr_t num_option_sets,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Returns static metadata about the `msw` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Msw::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo msw_info(void);

/**
 * Returns the minimum number of bars `msw` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (3) valid `f64`s.
 */
uintptr_t msw_min_data(const double *options);

/**
 * Runs `msw` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (3) values: `short_period, long_period,
 * cycle_period`.
 *
 * Returns the mandatory `msw` output plus any requested optional outputs
 * (`msw`, `trend`, in that fixed order) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult msw_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues an `msw` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `msw_indicator()` and not yet have been passed to `msw_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `msw_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult msw_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `msw_indicator()` (or one of the
 * `states[i]` entries from `msw_simd_by_assets()`/`msw_simd_by_options()`).
 * Call this once you're done streaming (after your last `msw_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `msw_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void msw_state_free(void *state);

/**
 * Computes MSW for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (3) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `msw` continuation state (reusable
 * with `msw_batch()`/`msw_state_free()`). Free each state via
 * `msw_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult msw_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Computes MSW for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (3)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `msw` continuation state
 * (reusable with `msw_batch()`/`msw_state_free()`). Free each state via
 * `msw_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult msw_simd_by_options(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *const *options,
                                       uintptr_t num_option_sets,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Returns static metadata about the `natr` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Natr::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo natr_info(void);

/**
 * Returns the minimum number of bars `natr` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t natr_min_data(const double *options);

/**
 * Runs `natr` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers, in order:
 * `high, low, close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `natr` output plus any requested optional outputs
 * (`atr`, `tr`, in that fixed order) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult natr_indicator(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Continues a `natr` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `natr_indicator()` and not yet have been passed to `natr_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `natr_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult natr_batch(void *state,
                               const double *const *inputs,
                               uintptr_t data_len,
                               const bool *optional_outputs,
                               uintptr_t numoptional);

/**
 * Frees a state handle returned by `natr_indicator()` (or one of the
 * `states[i]` entries from `natr_simd_by_assets()`/`natr_simd_by_options()`).
 * Call this once you're done streaming (after your last `natr_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `natr_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void natr_state_free(void *state);

/**
 * Computes NATR for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) shared value.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `natr` continuation state (reusable
 * with `natr_batch()`/`natr_state_free()`). Free each state via
 * `natr_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult natr_simd_by_assets(const double *const *const *inputs,
                                       uintptr_t num_assets,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Computes NATR for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`),
 * each `data_len` `f64`s long, shared across all option sets.
 * `options` must point to `num_option_sets` pointers, each itself
 * pointing to `OPTIONS` (1) value.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `natr` continuation state
 * (reusable with `natr_batch()`/`natr_state_free()`). Free each state
 * via `natr_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult natr_simd_by_options(const double *const *inputs,
                                        uintptr_t data_len,
                                        const double *const *options,
                                        uintptr_t num_option_sets,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Returns static metadata about the `nvi` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Nvi::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo nvi_info(void);

/**
 * Returns the minimum number of bars `nvi` needs to produce any output at
 * all. Since `nvi` has OPTIONS=0, no options are required.
 */
uintptr_t nvi_min_data(const double *_options);

/**
 * Runs `nvi` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (2) pointers, in order:
 * `close, volume`, each `data_len` `f64`s long.
 * `options` is ignored (OPTIONS=0).
 *
 * Returns the mandatory `nvi` output plus any requested optional outputs
 * and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult nvi_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *_options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues a `nvi` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `nvi_indicator()` and not yet have been passed to `nvi_state_free()`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`close, volume`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `nvi_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult nvi_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `nvi_indicator()` (or one of the
 * `states[i]` entries from `nvi_simd_by_assets()`).
 * Call this once you're done streaming (after your last `nvi_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `nvi_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void nvi_state_free(void *state);

/**
 * Computes NVI for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (2) pointers (`close, volume`), each `data_len` `f64`s long.
 * `options` is ignored (OPTIONS=0).
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `nvi` continuation state (reusable
 * with `nvi_batch()`/`nvi_state_free()`). Free each state via
 * `nvi_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult nvi_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *_options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Returns static metadata about the `obv` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Obv::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo obv_info(void);

/**
 * Returns the minimum number of bars `obv` needs to produce any output at
 * all. Since `obv` has OPTIONS=0, no options are required.
 */
uintptr_t obv_min_data(const double *_options);

/**
 * Runs `obv` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (2) pointers, in order:
 * `close, volume`, each `data_len` `f64`s long.
 * `options` is ignored (OPTIONS=0).
 *
 * Returns the mandatory `obv` output plus any requested optional outputs
 * and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult obv_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *_options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues an `obv` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `obv_indicator()` and not yet have been passed to `obv_state_free()`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`close, volume`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `obv_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult obv_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `obv_indicator()` (or one of the
 * `states[i]` entries from `obv_simd_by_assets()`).
 * Call this once you're done streaming (after your last `obv_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `obv_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void obv_state_free(void *state);

/**
 * Computes OBV for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (2) pointers (`close, volume`), each `data_len` `f64`s long.
 * `options` is ignored (OPTIONS=0).
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `obv` continuation state (reusable
 * with `obv_batch()`/`obv_state_free()`). Free each state via
 * `obv_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult obv_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *_options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Returns static metadata about the `pivotpoint` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `PivotPoint::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo pivotpoint_info(void);

/**
 * Returns the minimum number of bars `pivotpoint` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t pivotpoint_min_data(const double *options);

/**
 * Runs `pivotpoint` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers, in order:
 * `high, low, close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) valid `f64`: `period`.
 *
 * Returns the mandatory `pivotpoint` outputs (s3,s2,s1,pp,r1,r2,r3) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult pivotpoint_indicator(const double *const *inputs,
                                             uintptr_t data_len,
                                             const double *options,
                                             const bool *optional_outputs,
                                             uintptr_t numoptional);

/**
 * Continues a `pivotpoint` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `pivotpoint_indicator()` and not yet have been passed to `pivotpoint_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `pivotpoint_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult pivotpoint_batch(void *state,
                                     const double *const *inputs,
                                     uintptr_t data_len,
                                     const bool *optional_outputs,
                                     uintptr_t numoptional);

/**
 * Frees a state handle returned by `pivotpoint_indicator()` (or one of the
 * `states[i]` entries from `pivotpoint_simd_by_assets()` or `pivotpoint_simd_by_options()`).
 * Call this once you're done streaming (after your last `pivotpoint_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `pivotpoint_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void pivotpoint_state_free(void *state);

/**
 * Computes PivotPoint for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s long.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `pivotpoint` continuation state (reusable
 * with `pivotpoint_batch()`/`pivotpoint_state_free()`). Free each state via
 * `pivotpoint_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult pivotpoint_simd_by_assets(const double *const *const *inputs,
                                             uintptr_t num_assets,
                                             uintptr_t data_len,
                                             const double *options,
                                             const bool *optional_outputs,
                                             uintptr_t numoptional);

/**
 * Computes PivotPoint for `N` option sets simultaneously (SIMD-by-options).
 * `num_option_sets` must be 2, 4, 8, or 16 -- anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s long.
 * `options` must point to `num_option_sets` pointers, each itself pointing to
 * `OPTIONS` (1) valid `f64`s (the `period` value).
 *
 * Returns a `CSimdResult` with `num_option_sets` results. Free states via
 * `pivotpoint_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each pointing to
 *   `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult pivotpoint_simd_by_options(const double *const *inputs,
                                              uintptr_t data_len,
                                              const double *const *options,
                                              uintptr_t num_option_sets,
                                              const bool *optional_outputs,
                                              uintptr_t numoptional);

/**
 * Returns static metadata about the `ppo` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Ppo::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo ppo_info(void);

/**
 * Returns the minimum number of bars `ppo` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (2) valid `f64`s.
 */
uintptr_t ppo_min_data(const double *options);

/**
 * Runs `ppo` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer, in order:
 * `real`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (2) values: `short_period, long_period`.
 *
 * Returns the mandatory `ppo` output plus any requested optional outputs
 * (`short_ema`, `long_ema`, in that fixed order) and a fresh
 * continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult ppo_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues a `ppo` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `ppo_indicator()` and not yet have been passed to `ppo_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `ppo_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult ppo_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `ppo_indicator()` (or one of the
 * `states[i]` entries from `ppo_simd_by_assets()`/`ppo_simd_by_options()`).
 * Call this once you're done streaming (after your last `ppo_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `ppo_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void ppo_state_free(void *state);

/**
 * Computes PPO for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (2) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `ppo` continuation state (reusable
 * with `ppo_batch()`/`ppo_state_free()`). Free each state via
 * `ppo_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult ppo_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Computes PPO for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), each `data_len` `f64`s long,
 * shared across all option sets.
 * `options` must point to `num_option_sets` pointers, each itself
 * pointing to `OPTIONS` (2) values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `ppo` continuation state
 * (reusable with `ppo_batch()`/`ppo_state_free()`). Free each state
 * via `ppo_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult ppo_simd_by_options(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *const *options,
                                       uintptr_t num_option_sets,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Returns static metadata about the `psar` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Psar::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo psar_info(void);

/**
 * Returns the minimum number of bars `psar` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (2) valid `f64`s.
 */
uintptr_t psar_min_data(const double *options);

/**
 * Runs `psar` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (2) pointers, in order:
 * `high, low`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (2) values: `acceleration_factor, max_acceleration_factor`.
 *
 * Returns the mandatory `psar` output and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult psar_indicator(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Continues a `psar` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `psar_indicator()` and not yet have been passed to `psar_state_free()`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`high, low`), each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `psar_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult psar_batch(void *state,
                               const double *const *inputs,
                               uintptr_t data_len,
                               const bool *optional_outputs,
                               uintptr_t numoptional);

/**
 * Frees a state handle returned by `psar_indicator()` (or one of the
 * `states[i]` entries from `psar_simd_by_assets()`/`psar_simd_by_options()`).
 * Call this once you're done streaming (after your last `psar_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `psar_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void psar_state_free(void *state);

/**
 * Computes PSAR for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (2) pointers (`high, low`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (2) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `psar` continuation state (reusable
 * with `psar_batch()`/`psar_state_free()`). Free each state via
 * `psar_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult psar_simd_by_assets(const double *const *const *inputs,
                                       uintptr_t num_assets,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Computes PSAR for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`high, low`), each `data_len` `f64`s long,
 * shared across all option sets.
 * `options` must point to `num_option_sets` pointers, each itself
 * pointing to `OPTIONS` (2) values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `psar` continuation state
 * (reusable with `psar_batch()`/`psar_state_free()`). Free each state
 * via `psar_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult psar_simd_by_options(const double *const *inputs,
                                        uintptr_t data_len,
                                        const double *const *options,
                                        uintptr_t num_option_sets,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Returns static metadata about the `pvi` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Pvi::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo pvi_info(void);

/**
 * Returns the minimum number of bars `pvi` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (0) valid `f64`s.
 */
uintptr_t pvi_min_data(const double *_options);

/**
 * Runs `pvi` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (2) pointers, in order:
 * `close, volume`, each `data_len` `f64`s long.
 * `options` is ignored (PVI has no options).
 *
 * Returns the mandatory `pvi` output and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult pvi_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *_options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues a `pvi` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `pvi_indicator()` and not yet have been passed to `pvi_state_free()`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`close, volume`), each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `pvi_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult pvi_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `pvi_indicator()` (or one of the
 * `states[i]` entries from `pvi_simd_by_assets()`).
 * Call this once you're done streaming (after your last `pvi_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `pvi_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void pvi_state_free(void *state);

/**
 * Computes PVI for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (2) pointers (`close, volume`), each `data_len` `f64`s long.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `pvi` continuation state (reusable
 * with `pvi_batch()`/`pvi_state_free()`). Free each state via
 * `pvi_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult pvi_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *_options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Returns static metadata about the `qstick` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `QStick::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo qstick_info(void);

/**
 * Returns the minimum number of bars `qstick` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t qstick_min_data(const double *options);

/**
 * Runs `qstick` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (2) pointers, in order:
 * `open, close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) valid `f64`: `period`.
 *
 * Returns the mandatory `qstick` output (`qstick`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult qstick_indicator(const double *const *inputs,
                                         uintptr_t data_len,
                                         const double *options,
                                         const bool *optional_outputs,
                                         uintptr_t numoptional);

/**
 * Continues a `qstick` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `qstick_indicator()` and not yet have been passed to `qstick_state_free()`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`open, close`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) valid `f64`: `period`.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `qstick_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult qstick_batch(void *state,
                                 const double *const *inputs,
                                 uintptr_t data_len,
                                 const bool *optional_outputs,
                                 uintptr_t numoptional);

/**
 * Frees a state handle returned by `qstick_indicator()` (or one of the
 * `states[i]` entries from `qstick_simd_by_assets()` or `qstick_simd_by_options()`).
 * Call this once you're done streaming (after your last `qstick_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `qstick_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void qstick_state_free(void *state);

/**
 * Computes QStick for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (2) pointers (`open, close`), each `data_len` `f64`s long.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `qstick` continuation state (reusable
 * with `qstick_batch()`/`qstick_state_free()`). Free each state via
 * `qstick_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult qstick_simd_by_assets(const double *const *const *inputs,
                                         uintptr_t num_assets,
                                         uintptr_t data_len,
                                         const double *_options,
                                         const bool *optional_outputs,
                                         uintptr_t numoptional);

/**
 * Computes QStick for `N` option sets simultaneously (SIMD-by-options).
 * `num_option_sets` must be 2, 4, 8, or 16 -- anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`open, close`), each `data_len` `f64`s long.
 * `options` must point to `num_option_sets` pointers, each itself pointing to
 * `OPTIONS` (1) valid `f64`s (the `period` value).
 *
 * Returns a `CSimdResult` with `num_option_sets` results. Free states via
 * `qstick_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each pointing to
 *   `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult qstick_simd_by_options(const double *const *inputs,
                                          uintptr_t data_len,
                                          const double *const *_options,
                                          uintptr_t num_option_sets,
                                          const bool *optional_outputs,
                                          uintptr_t numoptional);

/**
 * Returns static metadata about the `roc` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Roc::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo roc_info(void);

/**
 * Returns the minimum number of bars `roc` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t roc_min_data(const double *options);

/**
 * Runs `roc` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) valid `f64`: `period`.
 *
 * Returns the mandatory `roc` output (`roc`) and optional output (`mom`),
 * plus a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult roc_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues a `roc` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `roc_indicator()` and not yet have been passed to `roc_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `roc_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult roc_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `roc_indicator()` (or one of the
 * `states[i]` entries from `roc_simd_by_assets()` or `roc_simd_by_options()`).
 * Call this once you're done streaming (after your last `roc_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `roc_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void roc_state_free(void *state);

/**
 * Computes ROC for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), each `data_len` `f64`s long.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `roc` continuation state (reusable
 * with `roc_batch()`/`roc_state_free()`). Free each state via
 * `roc_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult roc_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Computes ROC for `N` option sets simultaneously (SIMD-by-options).
 * `num_option_sets` must be 2, 4, 8, or 16 -- anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), each `data_len` `f64`s long.
 * `options` must point to `num_option_sets` pointers, each itself pointing to
 * `OPTIONS` (1) valid `f64`s (the `period` value).
 *
 * Returns a `CSimdResult` with `num_option_sets` results. Free states via
 * `roc_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each pointing to
 *   `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult roc_simd_by_options(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *const *options,
                                       uintptr_t num_option_sets,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Returns static metadata about the `rocr` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Rocr::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo rocr_info(void);

/**
 * Returns the minimum number of bars `rocr` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t rocr_min_data(const double *options);

/**
 * Runs `rocr` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) valid `f64`: `period`.
 *
 * Returns the mandatory `rocr` output (`rocr`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult rocr_indicator(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Continues a `rocr` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `rocr_indicator()` and not yet have been passed to `rocr_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `rocr_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult rocr_batch(void *state,
                               const double *const *inputs,
                               uintptr_t data_len,
                               const bool *optional_outputs,
                               uintptr_t numoptional);

/**
 * Frees a state handle returned by `rocr_indicator()` (or one of the
 * `states[i]` entries from `rocr_simd_by_assets()` or `rocr_simd_by_options()`).
 * Call this once you're done streaming (after your last `rocr_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `rocr_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void rocr_state_free(void *state);

/**
 * Computes ROCR for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), each `data_len` `f64`s long.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `rocr` continuation state (reusable
 * with `rocr_batch()`/`rocr_state_free()`). Free each state via
 * `rocr_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult rocr_simd_by_assets(const double *const *const *inputs,
                                       uintptr_t num_assets,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Computes ROCR for `N` option sets simultaneously (SIMD-by-options).
 * `num_option_sets` must be 2, 4, 8, or 16 -- anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), each `data_len` `f64`s long.
 * `options` must point to `num_option_sets` pointers, each itself pointing to
 * `OPTIONS` (1) valid `f64`s (the `period` value).
 *
 * Returns a `CSimdResult` with `num_option_sets` results. Free states via
 * `rocr_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each pointing to
 *   `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult rocr_simd_by_options(const double *const *inputs,
                                        uintptr_t data_len,
                                        const double *const *options,
                                        uintptr_t num_option_sets,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Returns static metadata about the `roofingfilter` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `RoofingFilter::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo roofingfilter_info(void);

/**
 * Returns the minimum number of bars `roofingfilter` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (2) valid `f64`s.
 */
uintptr_t roofingfilter_min_data(const double *options);

/**
 * Runs `roofingfilter` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (2) valid `f64`s: `ss_period, hp_period`.
 *
 * Returns the mandatory `roofingfilter` output (`roofing`) and optional output (`highpass`),
 * plus a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult roofingfilter_indicator(const double *const *inputs,
                                                uintptr_t data_len,
                                                const double *options,
                                                const bool *optional_outputs,
                                                uintptr_t numoptional);

/**
 * Continues a `roofingfilter` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `roofingfilter_indicator()` and not yet have been passed to `roofingfilter_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `roofingfilter_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult roofingfilter_batch(void *state,
                                        const double *const *inputs,
                                        uintptr_t data_len,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Frees a state handle returned by `roofingfilter_indicator()` (or one of the
 * `states[i]` entries from `roofingfilter_simd_by_assets()` or `roofingfilter_simd_by_options()`).
 * Call this once you're done streaming (after your last `roofingfilter_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `roofingfilter_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void roofingfilter_state_free(void *state);

/**
 * Computes RoofingFilter for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), each `data_len` `f64`s long.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `roofingfilter` continuation state (reusable
 * with `roofingfilter_batch()`/`roofingfilter_state_free()`). Free each state via
 * `roofingfilter_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult roofingfilter_simd_by_assets(const double *const *const *inputs,
                                                uintptr_t num_assets,
                                                uintptr_t data_len,
                                                const double *options,
                                                const bool *optional_outputs,
                                                uintptr_t numoptional);

/**
 * Computes RoofingFilter for `N` option sets simultaneously (SIMD-by-options).
 * `num_option_sets` must be 2, 4, 8, or 16 -- anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), each `data_len` `f64`s long.
 * `options` must point to `num_option_sets` pointers, each itself pointing to
 * `OPTIONS` (2) valid `f64`s (the `ss_period` and `hp_period` values).
 *
 * Returns a `CSimdResult` with `num_option_sets` results. Free states via
 * `roofingfilter_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each pointing to
 *   `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult roofingfilter_simd_by_options(const double *const *inputs,
                                                 uintptr_t data_len,
                                                 const double *const *options,
                                                 uintptr_t num_option_sets,
                                                 const bool *optional_outputs,
                                                 uintptr_t numoptional);

/**
 * Returns static metadata about the `rsi` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Rsi::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo rsi_info(void);

/**
 * Returns the minimum number of bars `rsi` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t rsi_min_data(const double *options);

/**
 * Runs `rsi` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointers, in order:
 * `real`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) valid `f64`s: `period`.
 *
 * Returns the mandatory `rsi` output and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult rsi_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues a `rsi` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `rsi_indicator()` and not yet have been passed to `rsi_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointers (`real`), each `data_len` `f64`s long.
 * The options used are those provided when the state was created via `rsi_indicator()`.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `rsi_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult rsi_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `rsi_indicator()` (or one of the
 * `states[i]` entries from `rsi_simd_by_assets()` or `rsi_simd_by_options()`).
 * Call this once you're done streaming (after your last `rsi_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `rsi_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void rsi_state_free(void *state);

/**
 * Computes RSI for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointers (`real`), each `data_len` `f64`s long.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `rsi` continuation state (reusable
 * with `rsi_batch()`/`rsi_state_free()`). Free each state via
 * `rsi_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult rsi_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Computes RSI with `N` different option sets on a single asset (SIMD).
 * `num_option_sets` must be 2, 4, 8, or 16 -- anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointers (`real`), each `data_len` `f64`s long.
 * `options` must point to `num_option_sets` pointers, each itself pointing to
 * `OPTIONS` (1) valid `f64`s.
 *
 * Returns a `CSimdResult` with `num_option_sets` results. Free states via
 * `rsi_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each pointing to
 *   `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult rsi_simd_by_options(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *const *options,
                                       uintptr_t num_option_sets,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Returns static metadata about the `sma` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Sma::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo sma_info(void);

/**
 * Returns the minimum number of bars `sma` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t sma_min_data(const double *options);

/**
 * Runs `sma` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointers, in order:
 * `real`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) valid `f64`s: `period`.
 *
 * Returns the mandatory `sma` output and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult sma_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues a `sma` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `sma_indicator()` and not yet have been passed to `sma_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointers (`real`), each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `sma_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult sma_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `sma_indicator()` (or one of the
 * `states[i]` entries from `sma_simd_by_assets()` or `sma_simd_by_options()`).
 * Call this once you're done streaming (after your last `sma_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `sma_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void sma_state_free(void *state);

/**
 * Computes SMA for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointers (`real`), each `data_len` `f64`s long.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `sma` continuation state (reusable
 * with `sma_batch()`/`sma_state_free()`). Free each state via
 * `sma_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult sma_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Computes SMA with `N` different option sets on a single asset (SIMD).
 * `num_option_sets` must be 2, 4, 8, or 16 -- anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointers (`real`), each `data_len` `f64`s long.
 * `options` must point to `num_option_sets` pointers, each itself pointing to
 * `OPTIONS` (1) valid `f64`s.
 *
 * Returns a `CSimdResult` with `num_option_sets` results. Free states via
 * `sma_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each pointing to
 *   `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult sma_simd_by_options(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *const *options,
                                       uintptr_t num_option_sets,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Returns static metadata about the `smaenvelope` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `SmaEnvelope::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo smaenvelope_info(void);

/**
 * Returns the minimum number of bars `smaenvelope` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (2) valid `f64`s.
 */
uintptr_t smaenvelope_min_data(const double *options);

/**
 * Runs `smaenvelope` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointers, in order:
 * `real`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (2) valid `f64`s: `period, percentage`.
 *
 * Returns the mandatory `smaenvelope` outputs (`lower, middle, upper`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult smaenvelope_indicator(const double *const *inputs,
                                              uintptr_t data_len,
                                              const double *options,
                                              const bool *optional_outputs,
                                              uintptr_t numoptional);

/**
 * Continues a `smaenvelope` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `smaenvelope_indicator()` and not yet have been passed to `smaenvelope_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointers (`real`), each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `smaenvelope_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult smaenvelope_batch(void *state,
                                      const double *const *inputs,
                                      uintptr_t data_len,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Frees a state handle returned by `smaenvelope_indicator()` (or one of the
 * `states[i]` entries from `smaenvelope_simd_by_assets()` or `smaenvelope_simd_by_options()`).
 * Call this once you're done streaming (after your last `smaenvelope_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `smaenvelope_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void smaenvelope_state_free(void *state);

/**
 * Computes SMA Envelope for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointers (`real`), each `data_len` `f64`s long.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `smaenvelope` continuation state (reusable
 * with `smaenvelope_batch()`/`smaenvelope_state_free()`). Free each state via
 * `smaenvelope_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult smaenvelope_simd_by_assets(const double *const *const *inputs,
                                              uintptr_t num_assets,
                                              uintptr_t data_len,
                                              const double *options,
                                              const bool *optional_outputs,
                                              uintptr_t numoptional);

/**
 * Computes SMA Envelope with `N` different option sets on a single asset (SIMD).
 * `num_option_sets` must be 2, 4, 8, or 16 -- anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointers (`real`), each `data_len` `f64`s long.
 * `options` must point to `num_option_sets` pointers, each itself pointing to
 * `OPTIONS` (2) valid `f64`s.
 *
 * Returns a `CSimdResult` with `num_option_sets` results. Free states via
 * `smaenvelope_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each pointing to
 *   `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult smaenvelope_simd_by_options(const double *const *inputs,
                                               uintptr_t data_len,
                                               const double *const *options,
                                               uintptr_t num_option_sets,
                                               const bool *optional_outputs,
                                               uintptr_t numoptional);

/**
 * Serialize a boxed indicator state into an opaque byte blob.
 * `state` is the `*mut c_void` handle from `<name>_indicator()` (READ ONLY
 * -- the state is not consumed). `indicator` must be the matching
 * `C_INDICATOR_ID_*` constant for that state's type. Returns
 * `CBytes { ptr: null }` on error. Free the blob with
 * `tulip_ffi_bytes_free`.
 *
 * The blob is self-describing (it embeds the indicator name); pass it
 * straight to `tulip_state_deserialize` with no further arguments.
 *
 * `format`: a `CStateFormat` discriminant (0 = Bincode, 1 = Json).
 *
 * # Safety
 * `state` must be null, or a valid pointer previously returned by an
 * indicator's `<name>_indicator()` function (or `tulip_state_deserialize`/
 * `tulip_state_clone`) that matches `indicator` and has not been passed to
 * `<name>_state_free()` yet. The state is read-only (not consumed).
 */
struct CBytes tulip_state_serialize(uint32_t indicator, uint32_t format, const void *state);

/**
 * Reconstruct a boxed state from a blob produced by `tulip_state_serialize`.
 * The blob's header identifies the indicator (by name) and the encoding
 * format -- there is no indicator argument to mismatch. The returned
 * `*mut c_void` is indistinguishable from a fresh `<name>_indicator()`
 * state handle: use with `<name>_batch()` and release with
 * `<name>_state_free()`.
 *
 * Returns null on error: null input, truncated data, bad magic, unknown
 * schema version, unknown format byte, unknown indicator name, or a
 * payload that fails to decode. Malformed input can never cause UB or a
 * panic -- only a null return.
 *
 * # Safety
 * `bytes` must be null, or point to `len` readable bytes. Memory safety
 * holds for *any* contents; the validity requirement is only that the
 * pointer/len pair is dereferenceable.
 */
void *tulip_state_deserialize(const uint8_t *bytes, uintptr_t len);

/**
 * Clone a boxed indicator state directly via Rust's `Clone` (core state
 * types all derive it -- no serde round-trip involved). Returns null on
 * error (null state, unknown indicator).
 * The cloned state is independent: use with `<name>_batch()` and release
 * with `<name>_state_free()`.
 *
 * # Safety
 * `state` must be null, or a valid pointer previously returned by an
 * indicator's `<name>_indicator()` function (or `tulip_state_deserialize`/
 * `tulip_state_clone`) that matches `indicator` and has not been passed to
 * `<name>_state_free()` yet.
 */
void *tulip_state_clone(uint32_t indicator, const void *state);

/**
 * Returns static metadata about the `stddev` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `StdDev::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo stddev_info(void);

/**
 * Returns the minimum number of bars `stddev` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t stddev_min_data(const double *options);

/**
 * Runs `stddev` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointers, in order:
 * `real`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) valid `f64`s: `period`.
 *
 * Returns the mandatory `stddev` output and optional `sma` output (if requested),
 * plus a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs; pass [true] + 1 to request sma).
 */
struct CIndicatorResult stddev_indicator(const double *const *inputs,
                                         uintptr_t data_len,
                                         const double *options,
                                         const bool *optional_outputs,
                                         uintptr_t numoptional);

/**
 * Continues a `stddev` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `stddev_indicator()` and not yet have been passed to `stddev_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointers (`real`), each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `stddev_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult stddev_batch(void *state,
                                 const double *const *inputs,
                                 uintptr_t data_len,
                                 const bool *optional_outputs,
                                 uintptr_t numoptional);

/**
 * Frees a state handle returned by `stddev_indicator()` (or one of the
 * `states[i]` entries from `stddev_simd_by_assets()` or `stddev_simd_by_options()`).
 * Call this once you're done streaming (after your last `stddev_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `stddev_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void stddev_state_free(void *state);

/**
 * Computes StdDev for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointers (`real`), each `data_len` `f64`s long.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `stddev` continuation state (reusable
 * with `stddev_batch()`/`stddev_state_free()`). Free each state via
 * `stddev_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult stddev_simd_by_assets(const double *const *const *inputs,
                                         uintptr_t num_assets,
                                         uintptr_t data_len,
                                         const double *options,
                                         const bool *optional_outputs,
                                         uintptr_t numoptional);

/**
 * Computes StdDev with `N` different option sets on a single asset (SIMD).
 * `num_option_sets` must be 2, 4, 8, or 16 -- anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointers (`real`), each `data_len` `f64`s long.
 * `options` must point to `num_option_sets` pointers, each itself pointing to
 * `OPTIONS` (1) valid `f64`s.
 *
 * Returns a `CSimdResult` with `num_option_sets` results. Free states via
 * `stddev_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each pointing to
 *   `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult stddev_simd_by_options(const double *const *inputs,
                                          uintptr_t data_len,
                                          const double *const *options,
                                          uintptr_t num_option_sets,
                                          const bool *optional_outputs,
                                          uintptr_t numoptional);

/**
 * Returns static metadata about the `stoch` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Stoch::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo stoch_info(void);

/**
 * Returns the minimum number of bars `stoch` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (3) valid `f64`s.
 */
uintptr_t stoch_min_data(const double *options);

/**
 * Runs `stoch` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers, in order:
 * `high, low, close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (3) valid `f64`s: `k_period, k_slow, d_period`.
 *
 * Returns the mandatory `stoch` outputs (`stoch_k, stoch_d`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult stoch_indicator(const double *const *inputs,
                                        uintptr_t data_len,
                                        const double *options,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Continues a `stoch` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `stoch_indicator()` and not yet have been passed to `stoch_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `stoch_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult stoch_batch(void *state,
                                const double *const *inputs,
                                uintptr_t data_len,
                                const bool *optional_outputs,
                                uintptr_t numoptional);

/**
 * Frees a state handle returned by `stoch_indicator()` (or one of the
 * `states[i]` entries from `stoch_simd_by_assets()` or `stoch_simd_by_options()`).
 * Call this once you're done streaming (after your last `stoch_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `stoch_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void stoch_state_free(void *state);

/**
 * Computes Stochastic Oscillator for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s long.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `stoch` continuation state (reusable
 * with `stoch_batch()`/`stoch_state_free()`). Free each state via
 * `stoch_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult stoch_simd_by_assets(const double *const *const *inputs,
                                        uintptr_t num_assets,
                                        uintptr_t data_len,
                                        const double *options,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Computes Stochastic Oscillator with `N` different option sets on a single asset (SIMD).
 * `num_option_sets` must be 2, 4, 8, or 16 -- anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s long.
 * `options` must point to `num_option_sets` pointers, each itself pointing to
 * `OPTIONS` (3) valid `f64`s.
 *
 * Returns a `CSimdResult` with `num_option_sets` results. Free states via
 * `stoch_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each pointing to
 *   `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult stoch_simd_by_options(const double *const *inputs,
                                         uintptr_t data_len,
                                         const double *const *options,
                                         uintptr_t num_option_sets,
                                         const bool *optional_outputs,
                                         uintptr_t numoptional);

/**
 * Returns static metadata about the `stochrsi` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `StochRsi::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo stochrsi_info(void);

/**
 * Returns the minimum number of bars `stochrsi` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t stochrsi_min_data(const double *options);

/**
 * Runs `stochrsi` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointers, in order:
 * `real`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) valid `f64`s: `period`.
 *
 * Returns the mandatory `stochrsi` output plus any requested optional outputs
 * (`rsi`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs; pass [true] + 1 to request rsi).
 */
struct CIndicatorResult stochrsi_indicator(const double *const *inputs,
                                           uintptr_t data_len,
                                           const double *options,
                                           const bool *optional_outputs,
                                           uintptr_t numoptional);

/**
 * Continues a `stochrsi` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `stochrsi_indicator()` and not yet have been passed to `stochrsi_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointers (`real`), each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `stochrsi_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult stochrsi_batch(void *state,
                                   const double *const *inputs,
                                   uintptr_t data_len,
                                   const bool *optional_outputs,
                                   uintptr_t numoptional);

/**
 * Frees a state handle returned by `stochrsi_indicator()` (or one of the
 * `states[i]` entries from `stochrsi_simd_by_assets()` or `stochrsi_simd_by_options()`).
 * Call this once you're done streaming (after your last `stochrsi_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `stochrsi_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void stochrsi_state_free(void *state);

/**
 * Computes Stochastic RSI for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointers (`real`), each `data_len` `f64`s long.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `stochrsi` continuation state (reusable
 * with `stochrsi_batch()`/`stochrsi_state_free()`). Free each state via
 * `stochrsi_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult stochrsi_simd_by_assets(const double *const *const *inputs,
                                           uintptr_t num_assets,
                                           uintptr_t data_len,
                                           const double *options,
                                           const bool *optional_outputs,
                                           uintptr_t numoptional);

/**
 * Computes Stochastic RSI with `N` different option sets on a single asset (SIMD).
 * `num_option_sets` must be 2, 4, 8, or 16 -- anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointers (`real`), each `data_len` `f64`s long.
 * `options` must point to `num_option_sets` pointers, each itself pointing to
 * `OPTIONS` (1) valid `f64`s.
 *
 * Returns a `CSimdResult` with `num_option_sets` results. Free states via
 * `stochrsi_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each pointing to
 *   `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult stochrsi_simd_by_options(const double *const *inputs,
                                            uintptr_t data_len,
                                            const double *const *options,
                                            uintptr_t num_option_sets,
                                            const bool *optional_outputs,
                                            uintptr_t numoptional);

/**
 * Returns static metadata about the `supersmoother` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `SuperSmoother::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo supersmoother_info(void);

/**
 * Returns the minimum number of bars `supersmoother` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t supersmoother_min_data(const double *options);

/**
 * Runs `supersmoother` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `supersmoother` output plus any requested optional outputs
 * (none for supersmoother) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult supersmoother_indicator(const double *const *inputs,
                                                uintptr_t data_len,
                                                const double *options,
                                                const bool *optional_outputs,
                                                uintptr_t numoptional);

/**
 * Continues an `supersmoother` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `supersmoother_indicator()` and not yet have been passed to `supersmoother_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `supersmoother_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult supersmoother_batch(void *state,
                                        const double *const *inputs,
                                        uintptr_t data_len,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Frees a state handle returned by `supersmoother_indicator()` (or one of the
 * `states[i]` entries from `supersmoother_simd_by_assets()`/`supersmoother_simd_by_options()`).
 * Call this once you're done streaming (after your last `supersmoother_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `supersmoother_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void supersmoother_state_free(void *state);

/**
 * Computes SuperSmoother for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `supersmoother` continuation state (reusable
 * with `supersmoother_batch()`/`supersmoother_state_free()`). Free each state via
 * `supersmoother_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult supersmoother_simd_by_assets(const double *const *const *inputs,
                                                uintptr_t num_assets,
                                                uintptr_t data_len,
                                                const double *options,
                                                const bool *optional_outputs,
                                                uintptr_t numoptional);

/**
 * Computes SuperSmoother for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `supersmoother` continuation state
 * (reusable with `supersmoother_batch()`/`supersmoother_state_free()`). Free each state via
 * `supersmoother_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult supersmoother_simd_by_options(const double *const *inputs,
                                                 uintptr_t data_len,
                                                 const double *const *options,
                                                 uintptr_t num_option_sets,
                                                 const bool *optional_outputs,
                                                 uintptr_t numoptional);

/**
 * Returns static metadata about the `supertrend` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `SuperTrend::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo supertrend_info(void);

/**
 * Returns the minimum number of bars `supertrend` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (2) valid `f64`s.
 */
uintptr_t supertrend_min_data(const double *options);

/**
 * Runs `supertrend` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers: `high`, `low`, `close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (2) values: `period`, `step`.
 *
 * Returns the mandatory `supertrend` output plus any requested optional outputs
 * (`atr`, `tr`, `medprice`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult supertrend_indicator(const double *const *inputs,
                                             uintptr_t data_len,
                                             const double *options,
                                             const bool *optional_outputs,
                                             uintptr_t numoptional);

/**
 * Continues an `supertrend` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `supertrend_indicator()` and not yet have been passed to `supertrend_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers: `high`, `low`, `close`, each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `supertrend_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult supertrend_batch(void *state,
                                     const double *const *inputs,
                                     uintptr_t data_len,
                                     const bool *optional_outputs,
                                     uintptr_t numoptional);

/**
 * Frees a state handle returned by `supertrend_indicator()` (or one of the
 * `states[i]` entries from `supertrend_simd_by_assets()`/`supertrend_simd_by_options()`).
 * Call this once you're done streaming (after your last `supertrend_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `supertrend_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void supertrend_state_free(void *state);

/**
 * Computes SuperTrend for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high`, `low`, `close`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (2) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `supertrend` continuation state (reusable
 * with `supertrend_batch()`/`supertrend_state_free()`). Free each state via
 * `supertrend_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult supertrend_simd_by_assets(const double *const *const *inputs,
                                             uintptr_t num_assets,
                                             uintptr_t data_len,
                                             const double *options,
                                             const bool *optional_outputs,
                                             uintptr_t numoptional);

/**
 * Computes SuperTrend for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high`, `low`, `close`), each `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (2)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `supertrend` continuation state
 * (reusable with `supertrend_batch()`/`supertrend_state_free()`). Free each state via
 * `supertrend_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult supertrend_simd_by_options(const double *const *inputs,
                                              uintptr_t data_len,
                                              const double *const *options,
                                              uintptr_t num_option_sets,
                                              const bool *optional_outputs,
                                              uintptr_t numoptional);

/**
 * Returns static metadata about the `tema` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Tema::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo tema_info(void);

/**
 * Returns the minimum number of bars `tema` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t tema_min_data(const double *options);

/**
 * Runs `tema` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `tema` output plus any requested optional outputs
 * (`dema`, `ema`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult tema_indicator(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Continues an `tema` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `tema_indicator()` and not yet have been passed to `tema_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `tema_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult tema_batch(void *state,
                               const double *const *inputs,
                               uintptr_t data_len,
                               const bool *optional_outputs,
                               uintptr_t numoptional);

/**
 * Frees a state handle returned by `tema_indicator()` (or one of the
 * `states[i]` entries from `tema_simd_by_assets()`/`tema_simd_by_options()`).
 * Call this once you're done streaming (after your last `tema_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `tema_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void tema_state_free(void *state);

/**
 * Computes TEMA for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `tema` continuation state (reusable
 * with `tema_batch()`/`tema_state_free()`). Free each state via
 * `tema_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult tema_simd_by_assets(const double *const *const *inputs,
                                       uintptr_t num_assets,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Computes TEMA for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `tema` continuation state
 * (reusable with `tema_batch()`/`tema_state_free()`). Free each state via
 * `tema_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult tema_simd_by_options(const double *const *inputs,
                                        uintptr_t data_len,
                                        const double *const *options,
                                        uintptr_t num_option_sets,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Returns static metadata about the `tr` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Tr::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo tr_info(void);

/**
 * Returns the minimum number of bars `tr` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * NOTE: OPTIONS == 0, no options needed. Pass null or any pointer.
 */
uintptr_t tr_min_data(const double *_options);

/**
 * Runs `tr` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers: `high`, `low`, `close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (0) values -- pass null or any pointer (ignored).
 *
 * Returns the mandatory `tr` output plus any requested optional outputs
 * (none for tr) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must be null or point to valid memory (ignored).
 * - `optional_outputs`, if non-null, must point to `num_optional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult tr_indicator(const double *const *inputs,
                                     uintptr_t data_len,
                                     const double *_options,
                                     const bool *optional_outputs,
                                     uintptr_t num_optional);

/**
 * Continues an `tr` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `tr_indicator()` and not yet have been passed to `tr_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers: `high`, `low`, `close`, each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `tr_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `num_optional` valid
 *   `bool`s.
 */
struct CBatchResult tr_batch(void *state,
                             const double *const *inputs,
                             uintptr_t data_len,
                             const bool *optional_outputs,
                             uintptr_t num_optional);

/**
 * Frees a state handle returned by `tr_indicator()` (or one of the
 * `states[i]` entries from `tr_simd_by_assets()`).
 * Call this once you're done streaming (after your last `tr_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `tr_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void tr_state_free(void *state);

/**
 * Computes TR for `N` assets simultaneously (SIMD). `num_assets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high`, `low`, `close`), `data_len` `f64`s long. Options are ignored.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `tr` continuation state (reusable
 * with `tr_batch()`/`tr_state_free()`). Free each state via
 * `tr_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must be null or point to valid memory (ignored).
 * - `optional_outputs`, if non-null, must point to `num_optional` valid
 *   `bool`s.
 */
struct CSimdResult tr_simd_by_assets(const double *const *const *inputs,
                                     uintptr_t num_assets,
                                     uintptr_t data_len,
                                     const double *_options,
                                     const bool *optional_outputs,
                                     uintptr_t num_optional);

/**
 * Returns static metadata about the `trendmode` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `TrendMode::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo trendmode_info(void);

/**
 * Returns the minimum number of bars `trendmode` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t trendmode_min_data(const double *options);

/**
 * Runs `trendmode` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `alpha`.
 *
 * Returns the mandatory `trendmode` output plus any requested optional outputs
 * (`cycle`, `peak`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `num_optional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult trendmode_indicator(const double *const *inputs,
                                            uintptr_t data_len,
                                            const double *options,
                                            const bool *optional_outputs,
                                            uintptr_t num_optional);

/**
 * Continues an `trendmode` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `trendmode_indicator()` and not yet have been passed to `trendmode_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `trendmode_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `num_optional` valid
 *   `bool`s.
 */
struct CBatchResult trendmode_batch(void *state,
                                    const double *const *inputs,
                                    uintptr_t data_len,
                                    const bool *optional_outputs,
                                    uintptr_t num_optional);

/**
 * Frees a state handle returned by `trendmode_indicator()` (or one of the
 * `states[i]` entries from `trendmode_simd_by_assets()`/`trendmode_simd_by_options()`).
 * Call this once you're done streaming (after your last `trendmode_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `trendmode_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void trendmode_state_free(void *state);

/**
 * Computes TrendMode for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `trendmode` continuation state (reusable
 * with `trendmode_batch()`/`trendmode_state_free()`). Free each state via
 * `trendmode_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `num_optional` valid
 *   `bool`s.
 */
struct CSimdResult trendmode_simd_by_assets(const double *const *const *inputs,
                                            uintptr_t num_assets,
                                            uintptr_t data_len,
                                            const double *options,
                                            const bool *optional_outputs,
                                            uintptr_t num_optional);

/**
 * Computes TrendMode for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `trendmode` continuation state
 * (reusable with `trendmode_batch()`/`trendmode_state_free()`). Free each state via
 * `trendmode_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `num_optional` valid
 *   `bool`s.
 */
struct CSimdResult trendmode_simd_by_options(const double *const *inputs,
                                             uintptr_t data_len,
                                             const double *const *options,
                                             uintptr_t num_option_sets,
                                             const bool *optional_outputs,
                                             uintptr_t num_optional);

/**
 * Returns static metadata about the `trima` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Trima::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo trima_info(void);

/**
 * Returns the minimum number of bars `trima` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t trima_min_data(const double *options);

/**
 * Runs `trima` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `trima` output plus any requested optional outputs
 * (none for trima) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult trima_indicator(const double *const *inputs,
                                        uintptr_t data_len,
                                        const double *options,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Continues a `trima` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `trima_indicator()` and not yet have been passed to `trima_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `trima_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult trima_batch(void *state,
                                const double *const *inputs,
                                uintptr_t data_len,
                                const bool *optional_outputs,
                                uintptr_t numoptional);

/**
 * Frees a state handle returned by `trima_indicator()` (or one of the
 * `states[i]` entries from `trima_simd_by_assets()`/`trima_simd_by_options()`).
 * Call this once you're done streaming (after your last `trima_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `trima_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void trima_state_free(void *state);

/**
 * Computes TRIMA for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `trima` continuation state (reusable
 * with `trima_batch()`/`trima_state_free()`). Free each state via
 * `trima_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult trima_simd_by_assets(const double *const *const *inputs,
                                        uintptr_t num_assets,
                                        uintptr_t data_len,
                                        const double *options,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Computes TRIMA for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `trima` continuation state
 * (reusable with `trima_batch()`/`trima_state_free()`). Free each state via
 * `trima_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult trima_simd_by_options(const double *const *inputs,
                                         uintptr_t data_len,
                                         const double *const *options,
                                         uintptr_t num_option_sets,
                                         const bool *optional_outputs,
                                         uintptr_t numoptional);

/**
 * Returns static metadata about the `trix` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Trix::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo trix_info(void);

/**
 * Returns the minimum number of bars `trix` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t trix_min_data(const double *options);

/**
 * Runs `trix` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `trix` output plus any requested optional outputs
 * (tema, dema, ema) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs; pass 3 bools to
 *   request tema/dema/ema).
 */
struct CIndicatorResult trix_indicator(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Continues a `trix` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `trix_indicator()` and not yet have been passed to `trix_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `trix_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult trix_batch(void *state,
                               const double *const *inputs,
                               uintptr_t data_len,
                               const bool *optional_outputs,
                               uintptr_t numoptional);

/**
 * Frees a state handle returned by `trix_indicator()` (or one of the
 * `states[i]` entries from `trix_simd_by_assets()`/`trix_simd_by_options()`).
 * Call this once you're done streaming (after your last `trix_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `trix_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void trix_state_free(void *state);

/**
 * Computes TRIX for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `trix` continuation state (reusable
 * with `trix_batch()`/`trix_state_free()`). Free each state via
 * `trix_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult trix_simd_by_assets(const double *const *const *inputs,
                                       uintptr_t num_assets,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Computes TRIX for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `trix` continuation state
 * (reusable with `trix_batch()`/`trix_state_free()`). Free each state via
 * `trix_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult trix_simd_by_options(const double *const *inputs,
                                        uintptr_t data_len,
                                        const double *const *options,
                                        uintptr_t num_option_sets,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Returns static metadata about the `trvi` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Trvi::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo trvi_info(void);

/**
 * Returns the minimum number of bars `trvi` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t trvi_min_data(const double *options);

/**
 * Runs `trvi` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers, in order:
 * `high`, `low`, `close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `trvi` output plus any requested optional outputs
 * (`tr`, `ema`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult trvi_indicator(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Continues a `trvi` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `trvi_indicator()` and not yet have been passed to `trvi_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `trvi_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult trvi_batch(void *state,
                               const double *const *inputs,
                               uintptr_t data_len,
                               const bool *optional_outputs,
                               uintptr_t numoptional);

/**
 * Frees a state handle returned by `trvi_indicator()` (or one of the
 * `states[i]` entries from `trvi_simd_by_assets()`/`trvi_simd_by_options()`).
 * Call this once you're done streaming (after your last `trvi_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `trvi_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void trvi_state_free(void *state);

/**
 * Computes TRVI for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high`, `low`, `close`), `data_len` `f64`s
 * long. `options` must point to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `trvi` continuation state (reusable
 * with `trvi_batch()`/`trvi_state_free()`). Free each state via
 * `trvi_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult trvi_simd_by_assets(const double *const *const *inputs,
                                       uintptr_t num_assets,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Computes TRVI for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (3) pointer (`high`, `low`, `close`),
 * `data_len` `f64`s long, shared across all option sets. `options` must point
 * to `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `trvi` continuation state
 * (reusable with `trvi_batch()`/`trvi_state_free()`). Free each state via
 * `trvi_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult trvi_simd_by_options(const double *const *inputs,
                                        uintptr_t data_len,
                                        const double *const *options,
                                        uintptr_t num_option_sets,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Returns static metadata about the `tsf` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Tsf::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo tsf_info(void);

/**
 * Returns the minimum number of bars `tsf` needs to produce any output at
 * all, given `options`.
 */
uintptr_t tsf_min_data(const double *options);

/**
 * Runs `tsf` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `tsf` output plus any requested optional outputs
 * (`linreg`, `linregslope`, `linregintercept`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult tsf_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues a `tsf` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `tsf_indicator()` and not yet have been passed to `tsf_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `tsf_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult tsf_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `tsf_indicator()` (or one of the
 * `states[i]` entries from `tsf_simd_by_assets()`/`tsf_simd_by_options()`).
 * Call this once you're done streaming (after your last `tsf_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `tsf_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void tsf_state_free(void *state);

/**
 * Computes TSF for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointers (`real`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) shared value: `period`.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `tsf` continuation state (reusable
 * with `tsf_batch()`/`tsf_state_free()`). Free each state via
 * `tsf_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 */
struct CSimdResult tsf_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Computes TSF for `N` option sets simultaneously (SIMD), using the same
 * inputs. `num_option_sets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointers (`real`), each `data_len` `f64`s long.
 * `options` must point to `num_option_sets` pointers, each itself pointing
 * to `OPTIONS` (1) value: `period`.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its own
 * output rows and its own ordinary `tsf` continuation state (reusable
 * with `tsf_batch()`/`tsf_state_free()`). Free each state via
 * `tsf_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each itself
 *   pointing to `OPTIONS` valid `f64`s.
 */
struct CSimdResult tsf_simd_by_options(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *const *options,
                                       uintptr_t num_option_sets,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Returns static metadata about the `typprice` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Typprice::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo typprice_info(void);

/**
 * Returns the minimum number of bars `typprice` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (0) valid `f64`s.
 */
uintptr_t typprice_min_data(const double *options);

/**
 * Runs `typprice` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers, in order:
 * `high, low, close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (0) values (pass null).
 *
 * Returns the mandatory `typprice` output plus any requested optional outputs
 * (none available) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s (or null if OPTIONS is 0).
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult typprice_indicator(const double *const *inputs,
                                           uintptr_t data_len,
                                           const double *options,
                                           const bool *optional_outputs,
                                           uintptr_t numoptional);

/**
 * Continues a `typprice` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `typprice_indicator()` and not yet have been passed to `typprice_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `typprice_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult typprice_batch(void *state,
                                   const double *const *inputs,
                                   uintptr_t data_len,
                                   const bool *optional_outputs,
                                   uintptr_t numoptional);

/**
 * Frees a state handle returned by `typprice_indicator()` (or one of the
 * `states[i]` entries from `typprice_simd_by_assets()`).
 * Call this once you're done streaming (after your last `typprice_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `typprice_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void typprice_state_free(void *state);

/**
 * Computes TYPPRICE for `N` assets simultaneously (SIMD), sharing a single
 * empty options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s
 * long. `options` must point to `OPTIONS` (0) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `typprice` continuation state (reusable
 * with `typprice_batch()`/`typprice_state_free()`). Free each state via
 * `typprice_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s (or null if OPTIONS is 0).
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult typprice_simd_by_assets(const double *const *const *inputs,
                                           uintptr_t num_assets,
                                           uintptr_t data_len,
                                           const double *options,
                                           const bool *optional_outputs,
                                           uintptr_t numoptional);

/**
 * Returns static metadata about the `ultosc` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Ultosc::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo ultosc_info(void);

/**
 * Returns the minimum number of bars `ultosc` needs to produce any output at
 * all, given `options`.
 */
uintptr_t ultosc_min_data(const double *options);

/**
 * Runs `ultosc` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers, in order:
 * `high, low, close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (3) values: `short_period, medium_period, long_period`.
 *
 * Returns the mandatory `ultosc` output plus any requested optional outputs
 * (`tr`, `bp`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult ultosc_indicator(const double *const *inputs,
                                         uintptr_t data_len,
                                         const double *options,
                                         const bool *optional_outputs,
                                         uintptr_t numoptional);

/**
 * Continues a `ultosc` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `ultosc_indicator()` and not yet have been passed to `ultosc_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `ultosc_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult ultosc_batch(void *state,
                                 const double *const *inputs,
                                 uintptr_t data_len,
                                 const bool *optional_outputs,
                                 uintptr_t numoptional);

/**
 * Frees a state handle returned by `ultosc_indicator()` (or one of the
 * `states[i]` entries from `ultosc_simd_by_assets()`/`ultosc_simd_by_options()`).
 * Call this once you're done streaming (after your last `ultosc_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `ultosc_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void ultosc_state_free(void *state);

/**
 * Computes ULTOSC for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (3) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `ultosc` continuation state (reusable
 * with `ultosc_batch()`/`ultosc_state_free()`). Free each state via
 * `ultosc_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 */
struct CSimdResult ultosc_simd_by_assets(const double *const *const *inputs,
                                         uintptr_t num_assets,
                                         uintptr_t data_len,
                                         const double *options,
                                         const bool *optional_outputs,
                                         uintptr_t numoptional);

/**
 * Computes ULTOSC for `N` option sets simultaneously (SIMD), using the same
 * inputs. `num_option_sets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s long.
 * `options` must point to `num_option_sets` pointers, each itself pointing
 * to `OPTIONS` (3) values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its own
 * output rows and its own ordinary `ultosc` continuation state (reusable
 * with `ultosc_batch()`/`ultosc_state_free()`). Free each state via
 * `ultosc_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each itself
 *   pointing to `OPTIONS` valid `f64`s.
 */
struct CSimdResult ultosc_simd_by_options(const double *const *inputs,
                                          uintptr_t data_len,
                                          const double *const *options,
                                          uintptr_t num_option_sets,
                                          const bool *optional_outputs,
                                          uintptr_t numoptional);

/**
 * Returns static metadata about the `vhf` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Vhf::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo vhf_info(void);

/**
 * Returns the minimum number of bars `vhf` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t vhf_min_data(const double *options);

/**
 * Runs `vhf` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `vhf` output plus any requested optional outputs
 * (none for this indicator) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 */
struct CIndicatorResult vhf_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues a `vhf` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `vhf_indicator()` and not yet have been passed to `vhf_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `vhf_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CBatchResult vhf_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `vhf_indicator()` (or one of the
 * `states[i]` entries from `vhf_simd_by_assets()`/`vhf_simd_by_options()`).
 * Call this once you're done streaming (after your last `vhf_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `vhf_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void vhf_state_free(void *state);

/**
 * Computes VHF for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), each `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `vhf` continuation state (reusable
 * with `vhf_batch()`/`vhf_state_free()`). Free each state via
 * `vhf_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 */
struct CSimdResult vhf_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Computes VHF for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `vhf` continuation state
 * (reusable with `vhf_batch()`/`vhf_state_free()`). Free each state via
 * `vhf_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 */
struct CSimdResult vhf_simd_by_options(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *const *options,
                                       uintptr_t num_option_sets,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Returns static metadata about the `vidya` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Vidya::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo vidya_info(void);

/**
 * Returns the minimum number of bars `vidya` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (3) valid `f64`s.
 */
uintptr_t vidya_min_data(const double *options);

/**
 * Runs `vidya` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (3) values: `short_period, long_period, alpha`.
 *
 * Returns the mandatory `vidya` output plus any requested optional outputs
 * (`short_sma, long_sma, short_stddev, long_stddev`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult vidya_indicator(const double *const *inputs,
                                        uintptr_t data_len,
                                        const double *options,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Continues a `vidya` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `vidya_indicator()` and not yet have been passed to `vidya_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `vidya_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CBatchResult vidya_batch(void *state,
                                const double *const *inputs,
                                uintptr_t data_len,
                                const bool *optional_outputs,
                                uintptr_t numoptional);

/**
 * Frees a state handle returned by `vidya_indicator()` (or one of the
 * `states[i]` entries from `vidya_simd_by_assets()`/`vidya_simd_by_options()`).
 * Call this once you're done streaming (after your last `vidya_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `vidya_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void vidya_state_free(void *state);

/**
 * Computes VIDYA for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), each `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (3) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `vidya` continuation state (reusable
 * with `vidya_batch()`/`vidya_state_free()`). Free each state via
 * `vidya_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult vidya_simd_by_assets(const double *const *const *inputs,
                                        uintptr_t num_assets,
                                        uintptr_t data_len,
                                        const double *options,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Computes VIDYA for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (3)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `vidya` continuation state
 * (reusable with `vidya_batch()`/`vidya_state_free()`). Free each state via
 * `vidya_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s.
 */
struct CSimdResult vidya_simd_by_options(const double *const *inputs,
                                         uintptr_t data_len,
                                         const double *const *options,
                                         uintptr_t num_option_sets,
                                         const bool *optional_outputs,
                                         uintptr_t numoptional);

/**
 * Returns static metadata about the `volatility` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Volatility::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo volatility_info(void);

/**
 * Returns the minimum number of bars `volatility` needs to produce any output at
 * all, given `options`.
 */
uintptr_t volatility_min_data(const double *options);

/**
 * Runs `volatility` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointers, in order:
 * `real`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) values: `period`.
 *
 * Returns the mandatory `volatility` output plus any requested optional outputs
 * (none for this indicator) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CIndicatorResult volatility_indicator(const double *const *inputs,
                                             uintptr_t data_len,
                                             const double *options,
                                             const bool *optional_outputs,
                                             uintptr_t numoptional);

/**
 * Continues a `volatility` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `volatility_indicator()` and not yet have been passed to `volatility_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointers (`real`), each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `volatility_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CBatchResult volatility_batch(void *state,
                                     const double *const *inputs,
                                     uintptr_t data_len,
                                     const bool *optional_outputs,
                                     uintptr_t numoptional);

/**
 * Frees a state handle returned by `volatility_indicator()` (or one of the
 * `states[i]` entries from `volatility_simd_by_assets()` or `volatility_simd_by_options()`).
 * Call this once you're done streaming (after your last `volatility_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `volatility_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void volatility_state_free(void *state);

/**
 * Computes Volatility for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointers (`real`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) shared values: `period`.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `volatility` continuation state (reusable
 * with `volatility_batch()`/`volatility_state_free()`). Free each state via
 * `volatility_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 */
struct CSimdResult volatility_simd_by_assets(const double *const *const *inputs,
                                             uintptr_t num_assets,
                                             uintptr_t data_len,
                                             const double *options,
                                             const bool *optional_outputs,
                                             uintptr_t numoptional);

/**
 * Computes Volatility for `N` option sets simultaneously (SIMD), using the same
 * inputs data. `num_option_sets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointers (`real`), each `data_len` `f64`s long.
 * `options` must point to `num_option_sets` arrays of `OPTIONS` (1) values: `period`.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its own
 * output rows and its own ordinary `volatility` continuation state (reusable
 * with `volatility_batch()`/`volatility_state_free()`). Free each state via
 * `volatility_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid arrays of `OPTIONS` `f64`s.
 */
struct CSimdResult volatility_simd_by_options(const double *const *inputs,
                                              uintptr_t data_len,
                                              const double *const *options,
                                              uintptr_t num_option_sets,
                                              const bool *optional_outputs,
                                              uintptr_t numoptional);

/**
 * Returns static metadata about the `vortex` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Vortex::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo vortex_info(void);

/**
 * Returns the minimum number of bars `vortex` needs to produce any output at
 * all, given `options`.
 */
uintptr_t vortex_min_data(const double *options);

/**
 * Runs `vortex` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers, in order:
 * `high, low, close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) values: `period`.
 *
 * Returns the mandatory `vortex` outputs (`vi_up, vi_down`) plus any requested optional outputs
 * (`tr`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CIndicatorResult vortex_indicator(const double *const *inputs,
                                         uintptr_t data_len,
                                         const double *options,
                                         const bool *optional_outputs,
                                         uintptr_t numoptional);

/**
 * Continues a `vortex` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `vortex_indicator()` and not yet have been passed to `vortex_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `vortex_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CBatchResult vortex_batch(void *state,
                                 const double *const *inputs,
                                 uintptr_t data_len,
                                 const bool *optional_outputs,
                                 uintptr_t numoptional);

/**
 * Frees a state handle returned by `vortex_indicator()` (or one of the
 * `states[i]` entries from `vortex_simd_by_assets()` or `vortex_simd_by_options()`).
 * Call this once you're done streaming (after your last `vortex_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `vortex_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void vortex_state_free(void *state);

/**
 * Computes Vortex for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) shared values: `period`.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `vortex` continuation state (reusable
 * with `vortex_batch()`/`vortex_state_free()`). Free each state via
 * `vortex_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 */
struct CSimdResult vortex_simd_by_assets(const double *const *const *inputs,
                                         uintptr_t num_assets,
                                         uintptr_t data_len,
                                         const double *options,
                                         const bool *optional_outputs,
                                         uintptr_t numoptional);

/**
 * Computes Vortex for `N` option sets simultaneously (SIMD), using the same
 * inputs data. `num_option_sets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s long.
 * `options` must point to `num_option_sets` arrays of `OPTIONS` (1) values: `period`.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its own
 * output rows and its own ordinary `vortex` continuation state (reusable
 * with `vortex_batch()`/`vortex_state_free()`). Free each state via
 * `vortex_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid arrays of `OPTIONS` `f64`s.
 */
struct CSimdResult vortex_simd_by_options(const double *const *inputs,
                                          uintptr_t data_len,
                                          const double *const *options,
                                          uintptr_t num_option_sets,
                                          const bool *optional_outputs,
                                          uintptr_t numoptional);

/**
 * Returns static metadata about the `vosc` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Vosc::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo vosc_info(void);

/**
 * Returns the minimum number of bars `vosc` needs to produce any output at
 * all, given `options`.
 */
uintptr_t vosc_min_data(const double *options);

/**
 * Runs `vosc` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointers, in order:
 * `volume`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (2) values: `short_period, long_period`.
 *
 * Returns the mandatory `vosc` output plus any requested optional outputs
 * (`short_sma, long_sma`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CIndicatorResult vosc_indicator(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Continues a `vosc` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `vosc_indicator()` and not yet have been passed to `vosc_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointers (`volume`), each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `vosc_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CBatchResult vosc_batch(void *state,
                               const double *const *inputs,
                               uintptr_t data_len,
                               const bool *optional_outputs,
                               uintptr_t numoptional);

/**
 * Frees a state handle returned by `vosc_indicator()` (or one of the
 * `states[i]` entries from `vosc_simd_by_assets()` or `vosc_simd_by_options()`).
 * Call this once you're done streaming (after your last `vosc_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `vosc_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void vosc_state_free(void *state);

/**
 * Computes VOSC for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointers (`volume`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (2) shared values: `short_period, long_period`.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `vosc` continuation state (reusable
 * with `vosc_batch()`/`vosc_state_free()`). Free each state via
 * `vosc_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 */
struct CSimdResult vosc_simd_by_assets(const double *const *const *inputs,
                                       uintptr_t num_assets,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Computes VOSC for `N` option sets simultaneously (SIMD), using the same
 * inputs data. `num_option_sets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointers (`volume`), each `data_len` `f64`s long.
 * `options` must point to `num_option_sets` arrays of `OPTIONS` (2) values: `short_period, long_period`.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its own
 * output rows and its own ordinary `vosc` continuation state (reusable
 * with `vosc_batch()`/`vosc_state_free()`). Free each state via
 * `vosc_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid arrays of `OPTIONS` `f64`s.
 */
struct CSimdResult vosc_simd_by_options(const double *const *inputs,
                                        uintptr_t data_len,
                                        const double *const *options,
                                        uintptr_t num_option_sets,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Returns static metadata about the `vwap` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Vwap::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo vwap_info(void);

/**
 * Returns the minimum number of bars `vwap` needs to produce any output at
 * all, given `options`.
 */
uintptr_t vwap_min_data(const double *_options);

/**
 * Runs `vwap` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (4) pointers, in order:
 * `high, low, close, volume`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (0) values (no options).
 *
 * Returns the mandatory `vwap` output plus any requested optional outputs
 * (`typprice`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CIndicatorResult vwap_indicator(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Continues a `vwap` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `vwap_indicator()` and not yet have been passed to `vwap_state_free()`.
 *
 * `inputs` must point to `INPUTS` (4) pointers (`high, low, close,
 * volume`), each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `vwap_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CBatchResult vwap_batch(void *state,
                               const double *const *inputs,
                               uintptr_t data_len,
                               const bool *optional_outputs,
                               uintptr_t numoptional);

/**
 * Frees a state handle returned by `vwap_indicator()` (or one of the
 * `states[i]` entries from `vwap_simd_by_assets()`).
 * Call this once you're done streaming (after your last `vwap_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `vwap_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void vwap_state_free(void *state);

/**
 * Computes VWAP for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (4) pointers (`high, low, close, volume`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (0) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `vwap` continuation state (reusable
 * with `vwap_batch()`/`vwap_state_free()`). Free each state via
 * `vwap_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 */
struct CSimdResult vwap_simd_by_assets(const double *const *const *inputs,
                                       uintptr_t num_assets,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Returns static metadata about the `vwma` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Vwma::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo vwma_info(void);

/**
 * Returns the minimum number of bars `vwma` needs to produce any output at
 * all, given `options`.
 */
uintptr_t vwma_min_data(const double *options);

/**
 * Runs `vwma` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (2) pointers, in order:
 * `close, volume`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) values: `period`.
 *
 * Returns the mandatory `vwma` output plus any requested optional outputs
 * (none for this indicator) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CIndicatorResult vwma_indicator(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Continues a `vwma` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `vwma_indicator()` and not yet have been passed to `vwma_state_free()`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`close, volume`), each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `vwma_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CBatchResult vwma_batch(void *state,
                               const double *const *inputs,
                               uintptr_t data_len,
                               const bool *optional_outputs,
                               uintptr_t numoptional);

/**
 * Frees a state handle returned by `vwma_indicator()` (or one of the
 * `states[i]` entries from `vwma_simd_by_assets()` or `vwma_simd_by_options()`).
 * Call this once you're done streaming (after your last `vwma_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `vwma_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void vwma_state_free(void *state);

/**
 * Computes VWMA for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (2) pointers (`close, volume`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) shared values: `period`.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `vwma` continuation state (reusable
 * with `vwma_batch()`/`vwma_state_free()`). Free each state via
 * `vwma_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 */
struct CSimdResult vwma_simd_by_assets(const double *const *const *inputs,
                                       uintptr_t num_assets,
                                       uintptr_t data_len,
                                       const double *options,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Computes VWMA for `N` option sets simultaneously (SIMD), using the same
 * inputs data. `num_option_sets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (2) pointers (`close, volume`), each `data_len` `f64`s long.
 * `options` must point to `num_option_sets` arrays of `OPTIONS` (1) values: `period`.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its own
 * output rows and its own ordinary `vwma` continuation state (reusable
 * with `vwma_batch()`/`vwma_state_free()`). Free each state via
 * `vwma_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid arrays of `OPTIONS` `f64`s.
 */
struct CSimdResult vwma_simd_by_options(const double *const *inputs,
                                        uintptr_t data_len,
                                        const double *const *options,
                                        uintptr_t num_option_sets,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Returns static metadata about the `wad` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Wad::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo wad_info(void);

/**
 * Returns the minimum number of bars `wad` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (0) valid `f64`s.
 */
uintptr_t wad_min_data(const double *_options);

/**
 * Runs `wad` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers, in order:
 * `high, low, close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (0) values (no options).
 *
 * Returns the mandatory `wad` output plus any requested optional outputs
 * (none for this indicator) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CIndicatorResult wad_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues a `wad` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `wad_indicator()` and not yet have been passed to `wad_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `wad_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CBatchResult wad_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `wad_indicator()` (or one of the
 * `states[i]` entries from `wad_simd_by_assets()`).
 * Call this once you're done streaming (after your last `wad_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `wad_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void wad_state_free(void *state);

/**
 * Computes WAD for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (0) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `wad` continuation state (reusable
 * with `wad_batch()`/`wad_state_free()`). Free each state via
 * `wad_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 */
struct CSimdResult wad_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Returns static metadata about the `wcprice` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `WcPrice::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo wcprice_info(void);

/**
 * Returns the minimum number of bars `wcprice` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (0) valid `f64`s.
 */
uintptr_t wcprice_min_data(const double *_options);

/**
 * Runs `wcprice` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers, in order:
 * `high, low, close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (0) values (no options).
 *
 * Returns the mandatory `wcprice` output plus any requested optional outputs
 * (none for this indicator) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CIndicatorResult wcprice_indicator(const double *const *inputs,
                                          uintptr_t data_len,
                                          const double *options,
                                          const bool *optional_outputs,
                                          uintptr_t numoptional);

/**
 * Continues a `wcprice` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `wcprice_indicator()` and not yet have been passed to `wcprice_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `wcprice_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CBatchResult wcprice_batch(void *state,
                                  const double *const *inputs,
                                  uintptr_t data_len,
                                  const bool *optional_outputs,
                                  uintptr_t numoptional);

/**
 * Frees a state handle returned by `wcprice_indicator()` (or one of the
 * `states[i]` entries from `wcprice_simd_by_assets()`).
 * Call this once you're done streaming (after your last `wcprice_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `wcprice_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void wcprice_state_free(void *state);

/**
 * Computes WCPRICE for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (0) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `wcprice` continuation state (reusable
 * with `wcprice_batch()`/`wcprice_state_free()`). Free each state via
 * `wcprice_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 */
struct CSimdResult wcprice_simd_by_assets(const double *const *const *inputs,
                                          uintptr_t num_assets,
                                          uintptr_t data_len,
                                          const double *options,
                                          const bool *optional_outputs,
                                          uintptr_t numoptional);

/**
 * Returns static metadata about the `wilders` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Wilders::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo wilders_info(void);

/**
 * Returns the minimum number of bars `wilders` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t wilders_min_data(const double *options);

/**
 * Runs `wilders` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `wilders` output plus any requested optional outputs
 * (none for this indicator) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 */
struct CIndicatorResult wilders_indicator(const double *const *inputs,
                                          uintptr_t data_len,
                                          const double *options,
                                          const bool *optional_outputs,
                                          uintptr_t numoptional);

/**
 * Continues a `wilders` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `wilders_indicator()` and not yet have been passed to `wilders_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `wilders_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CBatchResult wilders_batch(void *state,
                                  const double *const *inputs,
                                  uintptr_t data_len,
                                  const bool *optional_outputs,
                                  uintptr_t numoptional);

/**
 * Frees a state handle returned by `wilders_indicator()` (or one of the
 * `states[i]` entries from `wilders_simd_by_assets()` or `wilders_simd_by_options()`).
 * Call this once you're done streaming (after your last `wilders_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `wilders_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void wilders_state_free(void *state);

/**
 * Computes WILDERS for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `wilders` continuation state (reusable
 * with `wilders_batch()`/`wilders_state_free()`). Free each state via
 * `wilders_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 */
struct CSimdResult wilders_simd_by_assets(const double *const *const *inputs,
                                          uintptr_t num_assets,
                                          uintptr_t data_len,
                                          const double *options,
                                          const bool *optional_outputs,
                                          uintptr_t numoptional);

/**
 * Computes WILDERS with `num_option_sets` different option sets simultaneously (SIMD).
 * `num_option_sets` must be 2, 4, 8, or 16 -- anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s long.
 * `options` must point to `num_option_sets` pointers, each itself pointing to
 * `OPTIONS` (1) value(s). Each option set produces a separate result.
 *
 * Returns a `CSimdResult` with `num_option_sets` results. Free states via
 * `wilders_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to a valid non-null `*const f64` pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each pointing to `OPTIONS` valid `f64`s.
 */
struct CSimdResult wilders_simd_by_options(const double *const *inputs,
                                           uintptr_t data_len,
                                           const double *const *options,
                                           uintptr_t num_option_sets,
                                           const bool *optional_outputs,
                                           uintptr_t numoptional);

/**
 * Returns static metadata about the `willr` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Willr::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo willr_info(void);

/**
 * Returns the minimum number of bars `willr` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t willr_min_data(const double *options);

/**
 * Runs `willr` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (3) pointers, in order:
 * `high, low, close`, each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `willr` output plus any requested optional outputs
 * (`min`, `max`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult willr_indicator(const double *const *inputs,
                                        uintptr_t data_len,
                                        const double *options,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Continues a `willr` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `willr_indicator()` and not yet have been passed to `willr_state_free()`.
 *
 * `inputs` must point to `INPUTS` (3) pointers (`high, low, close`),
 * each `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `willr_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CBatchResult willr_batch(void *state,
                                const double *const *inputs,
                                uintptr_t data_len,
                                const bool *optional_outputs,
                                uintptr_t numoptional);

/**
 * Frees a state handle returned by `willr_indicator()` (or one of the
 * `states[i]` entries from `willr_simd_by_assets()` or `willr_simd_by_options()`).
 * Call this once you're done streaming (after your last `willr_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `willr_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void willr_state_free(void *state);

/**
 * Computes WILLR for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (3) pointers (`high, low, close`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `willr` continuation state (reusable
 * with `willr_batch()`/`willr_state_free()`). Free each state via
 * `willr_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 */
struct CSimdResult willr_simd_by_assets(const double *const *const *inputs,
                                        uintptr_t num_assets,
                                        uintptr_t data_len,
                                        const double *options,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Computes WILLR with `num_option_sets` different option sets simultaneously (SIMD).
 * `num_option_sets` must be 2, 4, 8, or 16 -- anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (3) pointer (`high, low, close`), `data_len` `f64`s long.
 * `options` must point to `num_option_sets` pointers, each itself pointing to
 * `OPTIONS` (1) value(s). Each option set produces a separate result.
 *
 * Returns a `CSimdResult` with `num_option_sets` results. Free states via
 * `willr_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to a valid non-null `*const f64` pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each pointing to `OPTIONS` valid `f64`s.
 */
struct CSimdResult willr_simd_by_options(const double *const *inputs,
                                         uintptr_t data_len,
                                         const double *const *options,
                                         uintptr_t num_option_sets,
                                         const bool *optional_outputs,
                                         uintptr_t numoptional);

/**
 * Returns static metadata about the `wma` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Wma::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo wma_info(void);

/**
 * Returns the minimum number of bars `wma` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t wma_min_data(const double *options);

/**
 * Runs `wma` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `wma` output plus any requested optional outputs
 * (`sma`) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 * - `optional_outputs`, if non-null, must point to `numoptional` valid
 *   `bool`s (pass null + 0 to request no optional outputs).
 */
struct CIndicatorResult wma_indicator(const double *const *inputs,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Continues a `wma` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `wma_indicator()` and not yet have been passed to `wma_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `wma_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CBatchResult wma_batch(void *state,
                              const double *const *inputs,
                              uintptr_t data_len,
                              const bool *optional_outputs,
                              uintptr_t numoptional);

/**
 * Frees a state handle returned by `wma_indicator()` (or one of the
 * `states[i]` entries from `wma_simd_by_assets()` or `wma_simd_by_options()`).
 * Call this once you're done streaming (after your last `wma_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `wma_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void wma_state_free(void *state);

/**
 * Computes WMA for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), each `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `wma` continuation state (reusable
 * with `wma_batch()`/`wma_state_free()`). Free each state via
 * `wma_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 */
struct CSimdResult wma_simd_by_assets(const double *const *const *inputs,
                                      uintptr_t num_assets,
                                      uintptr_t data_len,
                                      const double *options,
                                      const bool *optional_outputs,
                                      uintptr_t numoptional);

/**
 * Computes WMA with `num_option_sets` different option sets simultaneously (SIMD).
 * `num_option_sets` must be 2, 4, 8, or 16 -- anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s long.
 * `options` must point to `num_option_sets` pointers, each itself pointing to
 * `OPTIONS` (1) value(s). Each option set produces a separate result.
 *
 * Returns a `CSimdResult` with `num_option_sets` results. Free states via
 * `wma_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to a valid non-null `*const f64` pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each pointing to `OPTIONS` valid `f64`s.
 */
struct CSimdResult wma_simd_by_options(const double *const *inputs,
                                       uintptr_t data_len,
                                       const double *const *options,
                                       uintptr_t num_option_sets,
                                       const bool *optional_outputs,
                                       uintptr_t numoptional);

/**
 * Returns static metadata about the `zlema` indicator: its name, input
 * names, option names, and (mandatory/optional) output names, mirroring
 * `Zlema::INFO`.
 *
 * The returned strings are leaked, process-lifetime C strings -- read them,
 * don't free them.
 */
struct CIndicatorInfo zlema_info(void);

/**
 * Returns the minimum number of bars `zlema` needs to produce any output at
 * all, given `options`.
 *
 * # Safety
 * `options` must point to `OPTIONS` (1) valid `f64`s.
 */
uintptr_t zlema_min_data(const double *options);

/**
 * Runs `zlema` over `data_len` bars.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 * `options` must point to `OPTIONS` (1) value: `period`.
 *
 * Returns the mandatory `zlema` output plus any requested optional outputs
 * (none for this indicator) and a fresh continuation state.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 */
struct CIndicatorResult zlema_indicator(const double *const *inputs,
                                        uintptr_t data_len,
                                        const double *options,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Continues a `zlema` calculation from `state`, mutating it in place so
 * it's ready for the next call. `state` must have come from
 * `zlema_indicator()` and not yet have been passed to `zlema_state_free()`.
 *
 * `inputs` must point to `INPUTS` (1) pointer: `real`, `data_len` `f64`s long.
 *
 * # Safety
 * - `state` must be a live pointer previously returned by
 *   `zlema_indicator()`.
 * - `inputs` must point to `INPUTS` valid `*const f64`s, each pointing to
 *   `data_len` valid `f64`s.
 */
struct CBatchResult zlema_batch(void *state,
                                const double *const *inputs,
                                uintptr_t data_len,
                                const bool *optional_outputs,
                                uintptr_t numoptional);

/**
 * Frees a state handle returned by `zlema_indicator()` (or one of the
 * `states[i]` entries from `zlema_simd_by_assets()`/`zlema_simd_by_options()`).
 * Call this once you're done streaming (after your last `zlema_batch()` call).
 *
 * # Safety
 * `state` must be a live pointer previously returned by `zlema_indicator()`
 * or found in a `CSimdResult::states` array, and must not be used again
 * after this call.
 */
void zlema_state_free(void *state);

/**
 * Computes ZLEMA for `N` assets simultaneously (SIMD), sharing a single
 * options array. `num_assets` must be 2, 4, 8, or 16 -- anything else
 * returns `InvalidInputs`.
 *
 * `inputs` must point to `num_assets` pointers, each itself pointing to
 * `INPUTS` (1) pointer (`real`), each `data_len` `f64`s long. `options` must point
 * to `OPTIONS` (1) shared values.
 *
 * Returns a `CSimdResult` with `num_assets` results, each with its own
 * output rows and its own ordinary `zlema` continuation state (reusable
 * with `zlema_batch()`/`zlema_state_free()`). Free each state via
 * `zlema_state_free()`, then free the rest via `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `num_assets` valid pointers, each pointing to
 *   `INPUTS` valid non-null `*const f64`s, each pointing to `data_len` valid
 *   `f64`s.
 * - `options` must point to `OPTIONS` valid `f64`s.
 */
struct CSimdResult zlema_simd_by_assets(const double *const *const *inputs,
                                        uintptr_t num_assets,
                                        uintptr_t data_len,
                                        const double *options,
                                        const bool *optional_outputs,
                                        uintptr_t numoptional);

/**
 * Computes ZLEMA for one asset with `N` different option sets
 * simultaneously (SIMD). `num_option_sets` must be 2, 4, 8, or 16 --
 * anything else returns `InvalidInputs`.
 *
 * `inputs` must point to `INPUTS` (1) pointer (`real`), `data_len` `f64`s
 * long, shared across all option sets. `options` must point to
 * `num_option_sets` pointers, each itself pointing to `OPTIONS` (1)
 * values.
 *
 * Returns a `CSimdResult` with `num_option_sets` results, each with its
 * own output rows and its own ordinary `zlema` continuation state
 * (reusable with `zlema_batch()`/`zlema_state_free()`). Free each state via
 * `zlema_state_free()`, then free the rest via
 * `tulip_ffi_simd_result_free()`.
 *
 * # Safety
 * - `inputs` must point to `INPUTS` valid non-null `*const f64`s, each
 *   pointing to `data_len` valid `f64`s.
 * - `options` must point to `num_option_sets` valid pointers, each
 *   pointing to `OPTIONS` valid `f64`s.
 */
struct CSimdResult zlema_simd_by_options(const double *const *inputs,
                                         uintptr_t data_len,
                                         const double *const *options,
                                         uintptr_t num_option_sets,
                                         const bool *optional_outputs,
                                         uintptr_t numoptional);

#ifdef __cplusplus
}  // extern "C"
#endif  // __cplusplus

#endif  /* TULIP_RS_FFI_H */
